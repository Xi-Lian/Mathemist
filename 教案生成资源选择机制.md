# 教案生成资源选择机制详解

## 📋 概述

教案生成系统通过**智能资源检索**来确定使用哪些资源，整个过程分为多个层次，确保生成的教案质量高、相关性强。

## 🔄 完整流程

### 1. 意图识别阶段

**文件**: `backend/app/core/intent/service.py`

#### 判断是否为教案生成意图

系统通过以下方式识别教案生成意图：

```python
# 关键词匹配
INTENT_LESSON_PLAN = "generate_lesson_plan"
KEYWORDS = {
    INTENT_LESSON_PLAN: [
        "教案", "教学设计", "教学计划", "备课"
    ]
}

# 指令词匹配
INSTRUCTION_KEYWORDS = {
    "content_generation": ["生成", "设计", "写", "创作", "帮我做", "制作", "创建", "编写"]
}
```

**判断逻辑**：
- 如果用户输入包含"教案"等关键词 + "生成"等指令词 → `generate_lesson_plan`意图
- 如果用户使用"推送"、"找"、"推荐"等资源获取指令词 → `search`意图（拒绝生成教案）

### 2. 信息收集阶段

**文件**: `backend/app/core/unified_lesson_plan/methods/extract_lesson_plan_info.py`

从用户输入中提取关键信息：
- **课题** (topic): 教案主题
- **教学目标** (objectives)
- **授课对象** (audience): 年级/水平
- **课时安排** (duration)
- **教学方法** (teaching_method)
- **其他需求** (other_requirements)

### 3. 构建增强输入

**文件**: `backend/app/core/unified_lesson_plan/methods/build_enhanced_input.py`

将收集的信息整合成完整的查询字符串：

```python
enhanced_input = f"""
课题：{topic}
教学目标：{objectives}
授课对象：{audience}
课时安排：{duration}
教学方法：{teaching_method}
其他要求：{other_requirements}
"""
```

### 4. 资源检索阶段

**文件**: `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` (第32-47行)

#### 调用资源检索器

```python
retrieved_resources = self.resource_retriever.retrieve(
    enhanced_input,
    intent="generate_lesson_plan"  # ← 关键：指定意图
)
```

#### 检索器根据intent选择资源类型

**文件**: `backend/app/core/retrieval/methods/retrieve.py`

当`intent="generate_lesson_plan"`时，检索器会优先返回以下三类资源：

1. **理论资源** (theory_resources)
   - 数学理论卡片
   - 知识点说明
   - 概念定义

2. **教案示例** (lesson_plan_patterns)
   - 优秀教案模板
   - 教学设计示例
   - 教学方案参考

3. **优秀案例分析** (excellent_case_resources)
   - 实际教学案例
   - 教学反思
   - 教学效果分析

### 5. 资源过滤和限制

**文件**: `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` (第41-47行)

#### 数量限制策略

```python
# 限制资源数量，确保生成的教案更加聚焦
max_resources = 5      # 最多使用5个理论资源
max_patterns = 3       # 最多使用3个教案示例
max_excellent_cases = 3  # 最多使用3个优秀案例分析

theory_resources = theory_resources[:max_resources]
lesson_plan_patterns = lesson_plan_patterns[:max_patterns]
excellent_case_resources = excellent_case_resources[:max_excellent_cases]
```

**为什么要限制？**
- ✅ 避免LLM上下文过长
- ✅ 确保教案内容聚焦
- ✅ 提高生成质量
- ✅ 减少无关信息干扰

### 6. 资源评分和排序

检索器内部会对资源进行多维度评分：

#### 评分维度

1. **向量相似度** (base_relevance)
   - 基于embedding的语义相似度
   - 范围：0-1

2. **主题匹配度** (theme_match)
   - 是否匹配核心主题
   - 精确匹配 vs 相关匹配

3. **资源质量** (resource_quality)
   - 内容完整性
   - 结构规范性
   - 教学价值

4. **相关性提升** (theme_boost)
   - 主题关键词命中加分
   - 多主题匹配加分

#### 排序规则

资源按**综合得分**降序排列：
```python
overall_score = base_relevance * 0.4 + 
                theme_match_score * 0.3 + 
                resource_quality * 0.2 + 
                theme_boost * 0.1
```

取前N个最高分的资源。

### 7. 特殊处理规则

#### 教案资源的宽松阈值

**文件**: `backend/app/core/retrieval/merge_helpers/finalize.py` (第158-160行)

```python
# V303.0修复：为教案资源设置宽松阈值
if resource_type == "lesson_plan" or any(keyword in resource_type for keyword in ["教案", "教学设计"]):
    print("🔍 V303.0教案资源：使用宽松阈值 2.5")
    return 2.5  # 比其他资源类型更宽松
```

**为什么教案资源使用宽松阈值？**
- 教案内容通常较长，语义匹配可能不够精确
- 需要召回更多候选资源供后续筛选
- 避免因阈值过高而遗漏优质教案

#### 知识图谱过滤（习题资源）

对于习题资源，会使用知识图谱进行知识点一致性检查：

```python
# 检查习题的knowledge_points是否在KG扩展的主题关键词中
if not kg_keywords or any(kp in kg_keywords for kp in exercise_knowledge_points):
    # 保留该习题
else:
    # 降级为隐藏资源或丢弃
```

## 📊 资源选择决策树

```
用户输入
  ↓
[意图识别]
  ├─ generate_lesson_plan? → 继续
  └─ search? → 拒绝生成，执行资源检索
  
[信息提取]
  ↓
[构建增强输入]
  ↓
[调用ResourceRetriever.retrieve]
  ├─ intent="generate_lesson_plan"
  └─ query=enhanced_input
  ↓
[向量检索]
  ├─ 检索theory_resources
  ├─ 检索lesson_plan_patterns
  └─ 检索excellent_case_resources
  ↓
[评分和排序]
  ├─ 向量相似度
  ├─ 主题匹配度
  ├─ 资源质量
  └─ 相关性提升
  ↓
[数量限制]
  ├─ 理论资源：最多5条
  ├─ 教案示例：最多3条
  └─ 优秀案例：最多3条
  ↓
[返回给LLM生成教案]
```

## 🔑 关键代码位置

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| 意图识别 | `backend/app/core/intent/service.py` | 39-45 |
| 信息提取 | `backend/app/core/unified_lesson_plan/methods/extract_lesson_plan_info.py` | - |
| 构建增强输入 | `backend/app/core/unified_lesson_plan/methods/build_enhanced_input.py` | - |
| 资源检索调用 | `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` | 32-35 |
| 数量限制 | `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` | 41-47 |
| 资源评分 | `backend/app/core/retrieval/methods/calculate_overall_score.py` | - |
| 教案宽松阈值 | `backend/app/core/retrieval/merge_helpers/finalize.py` | 158-160 |
| KG过滤 | `backend/app/core/retrieval/methods/retrieve.py` | 634-695 |

## 💡 优化建议

### 1. 动态调整资源数量

当前是固定限制（5/3/3），可以根据课题复杂度动态调整：

```python
# 简单课题：减少资源数量
if complexity == "simple":
    max_resources = 3
    max_patterns = 2
    max_excellent_cases = 2

# 复杂课题：增加资源数量
elif complexity == "complex":
    max_resources = 8
    max_patterns = 5
    max_excellent_cases = 5
```

### 2. 资源多样性保证

确保不同类型的资源都有代表：

```python
# 确保至少包含不同教学方法的教案示例
teaching_methods = set()
for pattern in lesson_plan_patterns:
    methods = pattern.get('teaching_methods', [])
    teaching_methods.update(methods)

if len(teaching_methods) < 2:
    # 补充更多样化的教案示例
    additional_patterns = retrieve_diverse_patterns(core_theme)
    lesson_plan_patterns.extend(additional_patterns)
```

### 3. 资源时效性优先

优先使用最近更新的资源：

```python
# 按更新时间排序
lesson_plan_patterns.sort(
    key=lambda x: x.get('update_time', ''),
    reverse=True
)
```

### 4. 用户偏好学习

记录用户对哪些资源满意，优化未来的资源选择：

```python
# 在session中记录用户反馈
session['resource_feedback'] = {
    'liked_resources': [...],
    'disliked_resources': [...]
}

# 下次生成时优先使用类似资源
```

## 🎯 总结

教案生成的资源选择是一个**多层次、多维度**的智能决策过程：

1. ✅ **意图驱动**: 根据`intent="generate_lesson_plan"`选择特定资源类型
2. ✅ **语义匹配**: 基于向量相似度找到最相关的资源
3. ✅ **质量评估**: 多维度评分确保资源质量
4. ✅ **数量控制**: 限制资源数量保证聚焦和质量
5. ✅ **特殊优化**: 教案资源使用宽松阈值，提高召回率

这种设计确保了生成的教案：
- 📚 **理论基础扎实**: 有充分的理论支撑
- 📝 **结构规范**: 参考优秀教案示例
- 💡 **实践指导性强**: 结合真实教学案例
- 🎯 **针对性强**: 紧扣用户需求和课题主题
