# V43.2 教学用途严格降级过滤 - 确保按优先级返回资源

## 📋 问题背景

用户反馈："没有复习课，有练习课就要输出练习课，实在两个都没有再输出新授课"

**之前的问题**：
- 即使数据库中有练习课课件，也可能因为向量检索召回率问题没有被检索到
- 或者即使被检索到，也可能因为评分排序问题，新授课课件排在了前面
- 导致用户看到的是新授课课件，而不是更合适的练习课课件

## 🔧 V43.2改进内容

### 核心改进：在过滤阶段实施严格的降级策略

**修改文件**：`backend/app/core/response/methods/filter_by_relevance.py`

### 1. 检测用户意图

```python
user_input = self._get_state_value(getattr(self, 'state', None), "user_input", "")
user_intent = None
if user_input:
    if '复习' in user_input or '总结' in user_input or '回顾' in user_input:
        user_intent = '复习课'
    elif '练习' in user_input or '习题' in user_input or '训练' in user_input:
        user_intent = '练习课'
    elif '新授' in user_input or '新课' in user_input:
        user_intent = '新授课'
```

### 2. 实施严格降级过滤

```python
def _apply_teaching_use_fallback_filter(self, resources, user_intent):
    """
    V43.2：实施教学用途降级过滤
    
    规则：
    1. 优先返回复习课课件
    2. 如果没有复习课，但有练习课 → 只返回练习课（不返回新授课）
    3. 如果复习课和练习课都没有 → 才返回新授课
    """
    
    # 定义降级优先级
    fallback_priority = {
        '复习课': ['复习课', '练习课', '习题课', '新授课'],
        '练习课': ['练习课', '习题课', '新授课'],
        '习题课': ['习题课', '练习课', '新授课'],
        '新授课': ['新授课'],
    }
    
    # 按优先级分组资源
    priority_groups = {}
    for target in allowed_targets:
        priority_groups[target] = []
    
    for resource in resources:
        teaching_use = resource.get('teaching_use', '')
        
        # 检查资源属于哪个优先级
        for target in allowed_targets:
            if target in teaching_use:
                priority_groups[target].append(resource)
                break
    
    # 按优先级顺序选择资源
    for target in allowed_targets:
        if priority_groups[target]:
            return priority_groups[target]  # ✅ 找到就立即返回，不再检查更低优先级
    
    return []
```

## 📊 实际效果对比

### 场景：用户查询"我要给学生复习余弦定理，给我相关课件"

假设数据库中：
- 复习课课件：0个
- 练习课课件：2个（`课时1 余弦定理`、`课时4 余弦定理应用举例`）
- 新授课课件：8个

#### V43.1（之前）
```
【课件资源】

⭐ 【核心主题匹配】（5个）：

**📊 ⭐ [新授] 课时14_第六章平面向量及其应用_6.4.3余弦定理...**
- 适配说明：核心匹配；主题：余弦定理；课件三字段评估得分: 0.735
- ⚠️ 这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）
```

**问题**：
- ❌ 返回的是新授课课件
- ❌ 虽然有提示，但用户可能更希望看到练习课课件
- ❌ 练习课课件可能被检索到了，但因为评分或排序问题没有显示

#### V43.2（现在）
```
【课件资源】

⭐ 【核心主题匹配】（2个）：

**📊 ⭐ [练习] 课时1 余弦定理**
- 适配说明：核心匹配；主题：余弦定理
- 内容预览：...
- 文件路径：[打开文件](...)

**📊 ⭐ [练习] 课时4 余弦定理、正弦定理应用举例**
- 适配说明：核心匹配；主题：余弦定理
- 内容预览：...
- 文件路径：[打开文件](...)
```

**改进**：
- ✅ **只返回练习课课件**（因为有练习课）
- ✅ **不返回新授课课件**（因为有更合适的练习课）
- ✅ 符合用户的期望："有练习课就要输出练习课"

### 场景2：如果连练习课都没有

假设数据库中：
- 复习课课件：0个
- 练习课课件：0个
- 新授课课件：8个

#### V43.2结果
```
【课件资源】

⭐ 【核心主题匹配】（5个）：

**📊 ⭐ [新授] 课时14_第六章平面向量及其应用_6.4.3余弦定理...**
- 适配说明：核心匹配；主题：余弦定理；课件三字段评估得分: 0.3
- ⚠️ 这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）
```

**说明**：
- ✅ 只有在完全没有复习课和练习课时，才返回新授课
- ✅ 仍然保留V43.1的用户提示功能

## 🎯 优势分析

### 1. 严格按优先级返回
- **复习课 > 练习课 > 新授课**
- 不会跳过中间层级直接返回更低优先级的资源

### 2. 提高用户满意度
- 用户明确要求"复习"时，优先提供练习课（更适合复习）
- 避免返回不太合适的新授课课件

### 3. 保持透明度
- 仍然保留V43.1的用户提示功能
- 当返回降级资源时，明确告知用户原因

### 4. 解决检索召回率问题
- 即使在向量检索阶段练习课课件得分较低
- 在过滤阶段也会根据教学用途重新筛选
- 确保最合适的资源被返回

## 📝 实施步骤

### 1. 修改过滤逻辑
✅ 已完成：`filter_by_relevance.py`中的`_filter_by_relevance`方法
✅ 已完成：添加`_apply_teaching_use_fallback_filter`方法

### 2. 重启服务
需要重启后端服务以加载新代码：
```powershell
cd d:\Git_Repository\Mathemist
.\start-local.ps1
```

### 3. 测试验证

#### 测试用例1：有余弦定理练习课课件
查询："我要给学生复习余弦定理，给我相关课件"

预期结果：
- ✅ 返回练习课课件（如果有）
- ✅ 不返回新授课课件（如果有练习课）
- ✅ 如果没有练习课，才返回新授课

#### 测试用例2：只有新授课课件
查询："我要给学生复习余弦函数，给我相关课件"

预期结果：
- ✅ 返回新授课课件
- ✅ 显示降级提示："这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）"

## 🔮 未来优化方向

### 1. 优化向量检索
- 提高练习课课件的召回率
- 确保所有相关资源都能进入评估阶段

### 2. 动态调整降级策略
- 根据用户反馈调整降级优先级
- 例如：某些主题下，新授课可能比练习课更适合复习

### 3. 智能推荐
- 当检测到降级时，主动建议用户
- 例如："未找到复习课课件，已为您找到2个练习课课件"

### 4. 数据补充
- 为常用主题添加复习课课件
- 减少降级的必要性

## 📌 注意事项

1. **必须重启服务**：Python模块被缓存，修改后必须重启才能生效
2. **清除浏览器缓存**：确保前端显示最新的结果格式
3. **监控日志**：观察`[DEBUG] V43.2`日志，确认降级过滤是否正常工作
4. **持续优化数据**：降级策略是临时方案，长期目标是完善数据

---

**版本**：V43.2  
**日期**：2026-05-16  
**作者**：Lingma AI Assistant  
**相关文件**：
- `backend/app/core/response/methods/filter_by_relevance.py`
- `backend/app/core/retrieval/evaluation/courseware_evaluator.py`（V43.1）
- `backend/app/core/response/methods/append_resource_info.py`（V43.1）
