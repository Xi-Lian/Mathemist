from .._shared import *


class _ExtractThemeWithLlmMixin:
    def _extract_theme_with_llm(self, query: str, has_resource_type: bool = False, intent: str = "", is_exercise: bool = False) -> tuple:
        """
        使用LLM动态提取主题和板块
        
        Args:
            query: 用户查询
            has_resource_type: 查询是否包含资源类型词
            intent: 查询意图
            is_exercise: 是否是习题检索（如果是，需要识别组合查询）
            
        Returns:
            (提取的主题字符串, 板块名称)
        """
        from langchain_core.prompts import ChatPromptTemplate
        from langchain_core.output_parsers import StrOutputParser
        
        # 构建提示词
        resource_type_hint = "包含" if has_resource_type else "不包含"
        can_extract_broad = "可以" if has_resource_type else "不可以"
        
        # 【V65.2新增】如果是习题检索，使用特殊的提示词来识别组合查询
        if is_exercise:
            prompt = ChatPromptTemplate.from_messages([
                ("system", f"""你是一个高中数学主题识别专家。你的任务是从用户查询中提取核心数学主题，并判断每个主题属于函数、几何、概率统计、代数中的哪一个板块。

4个板块：函数、几何、概率统计、代数

【极其重要 - 输出格式】：
- 你的输出必须严格遵循格式：主题:板块
- 【禁止】在输出中使用逗号！即使有多个主题，也不要用逗号分隔！
- 【禁止】输出任何解释、说明或其他文字！
- 只能输出：主题:板块

【极其重要 - 复合知识点的识别】：
用户查询中的"的"表示修饰关系，不是分隔关系！

✅ 正确理解（修饰关系，不需要拆分）：
- "分段函数单调性" = "分段函数的单调性" → 是一个整体！
- "二次函数单调性" = "二次函数的单调性" → 是一个整体！
- "函数单调性的区间判断" = "函数单调性在区间上的判断" → 是一个整体！
- "指数函数奇偶性" = "指数函数的奇偶性" → 是一个整体！

❌ 错误理解（不要这样拆分）：
- "分段函数单调性" ≠ "分段函数" + "单调性"（这是两个独立概念）
- "二次函数单调性" ≠ "二次函数" + "单调性"（这是两个独立概念）

【重要区分 - 什么时候才需要输出多个主题】：
只有当用户查询中使用了以下连接词时，才认为是多个独立主题：
- "和"：如"指数函数和对数函数" → 两个独立的函数类型
- "与"：如"线面平行与面面平行" → 两个独立的性质
- "及"：如"数列及数列极限" → 两个独立的章节
- "或者"/"或"：如"正弦函数或者余弦函数" → 两个可选择的函数

⚠️ 特别注意：
- "分段函数单调性"中没有"和"、"与"、"及"、"或者"，所以是一个主题！
- "二次函数在区间的单调性"中没有连接词，是一个主题！
- 只有"分段函数和二次函数的单调性"才是两个主题！

【规则】：
1. 【最高优先级】提取最具体，最细分的主题（例如："函数的单调性"比"函数"更具体，"函数单调性的性质应用"比"函数单调性"更具体）
2. 只有当用户查询中使用了"和"、"与"、"及"、"或者"等明确的并列连接词时，才拆分为多个主题
3. 对于每个主题，判断其所属的板块，用冒号分隔主题和板块
4. 【严格禁止】绝对不要提取过于宽泛的主题，如单独的"函数"、"数学"、"几何"等，即使查询包含资源类型词（如"选择题"、"习题"等）
5. 重要：忽略查询中的修饰词，如"基础一点的"、"中等难度的"、"高二的"、"难一点的"等，只提取核心数学主题
6. 重要：充分发挥你的数学知识判断能力，将知识点归入最合适的板块

示例：
- 输入："分段函数单调性的习题" → 输出："分段函数单调性:函数"
- 输入："二次函数单调性的练习题" → 输出："二次函数单调性:函数"
- 输入："指数函数奇偶性的题目" → 输出："指数函数奇偶性:函数"
- 输入："函数单调性的区间判断的题" → 输出："函数单调性的区间判断:函数"
- 输入："三角函数图像的变换的习题" → 输出："三角函数图像的变换:函数"
- 输入："立体几何中线面垂直的证明的题" → 输出："立体几何中线面垂直的证明:几何"
- 输入："指数函数和对数函数的教案" → 输出："指数函数:函数,对数函数:函数"
- 输入："线面平行与面面平行的习题" → 输出："线面平行:几何,面面平行:几何"
- 输入："关于函数单调性的教案" → 输出："函数的单调性:函数"
- 输入："给我找一些三角函数诱导公式的习题" → 输出："诱导公式:函数"
- 输入："平面的教案" → 输出："平面:几何"
- 输入："空间点的教案" → 输出："空间点:几何"
- 输入："关于分层抽样的教案" → 输出："分层抽样:概率统计"
- 输入："复数的教案" → 输出："复数:代数"
- 输入："分类加法计数原理的练习课课件" → 输出："分类加法计数原理:概率统计"
- 输入："立体几何练习题" → 输出："立体几何:几何"

附加信息：原始查询{resource_type_hint}资源类型词，因此{can_extract_broad}提取通用主题如"函数"作为核心主题。"""),
                ("user", "用户查询：{query}\n\n请提取核心主题和所属板块（格式：主题:板块）：")
            ])
        else:
            # 非习题检索，使用原有提示词
            prompt = ChatPromptTemplate.from_messages([
                ("system", f"""你是一个高中数学主题识别专家。你的任务是从用户查询中提取核心数学主题，并判断每个主题属于函数、几何、概率统计、代数中的哪一个板块。

4个板块：函数、几何、概率统计、代数

规则：
1. 提取最具体，最细分的主题（例如："函数的单调性"比"函数"更具体）
2. 如果查询包含多个主题，用逗号分隔
3. 对于每个主题，判断其所属的板块，用冒号分隔主题和板块
4. 输出格式：主题1:板块1,主题2:板块2
5. 只返回主题和板块，不要解释，不要添加任何其他文字
6. 必须返回有效的主题和板块，不能返回空字符串
7. 重要：不要提取过于宽泛的主题，如单独的"函数"、"数学"等，除非查询包含资源类型词（如"选择题"、"习题"等）或有明确的查询意图
8. 重要：忽略查询中的修饰词，如"基础一点的"、"中等难度的"、"高二的"、"难一点的"等，只提取核心数学主题
9. 重要：充分发挥你的数学知识判断能力，将知识点归入最合适的板块

示例：
- 输入："关于函数单调性的教案" -> 输出："函数的单调性:函数"
- 输入："给我找一些三角函数诱导公式的习题" -> 输出："诱导公式:函数"
- 输入："指数函数和对数函数的教案" -> 输出："指数函数:函数,对数函数:函数"
- 输入："平面的教案" -> 输出："平面:几何"
- 输入："空间点的教案" -> 输出："空间点:几何"
- 输入："关于分层抽样的教案" -> 输出："分层抽样:概率统计"
- 输入："复数的教案" -> 输出："复数:代数"
- 输入："分类加法计数原理的练习课课件" -> 输出："分类加法计数原理:概率统计"
- 输入："立体几何练习题" -> 输出："立体几何:几何"

附加信息：原始查询{resource_type_hint}资源类型词，因此{can_extract_broad}提取通用主题如"函数"作为核心主题。"""),
                ("user", "用户查询：{query}\n\n请提取核心主题和所属板块（格式：主题1:板块1,主题2:板块2）：")
            ])
        
        # 获取模型
        model = self.model_config.get_model("intent")
        
        # 构建链
        chain = prompt | model | StrOutputParser()
        
        # 调用模型
        result = chain.invoke({"query": query})
        
        # 清理结果
        result = result.strip()
        print(f"🤖 LLM原始输出: '{result}'")
        
        if result and len(result) > 0:
            # 解析输出格式
            themes_with_boards = []
            themes = []
            boards = []
            
            # 分割多个主题
            theme_board_pairs = result.split(',')
            for pair in theme_board_pairs:
                if ':' in pair:
                    theme, board = pair.split(':', 1)
                    theme = theme.strip()
                    board = board.strip()
                    if theme and board:
                        themes_with_boards.append((theme, board))
                        themes.append(theme)
                        boards.append(board)
            
            if themes_with_boards:
                # 返回主题字符串和主要板块（使用第一个主题的板块）
                main_board = boards[0] if boards else None
                themes_str = ','.join(themes)
                return themes_str, main_board
            else:
                print(f"⚠️ LLM输出格式错误: '{result}'，使用关键词匹配")

        print(f"⚠️ LLM返回空或无效结果，使用关键词匹配")
        return "", None
