# 项目文件清理建议

## 📊 清理概览

**预计可删除文件数量**：约 **150+ 个文件**  
**预计释放空间**：约 **50-100 MB**（不包括数据库和缓存）

---

## 🗑️ 第一类：根目录临时脚本（约60个文件）

### 可以安全删除的调试/测试脚本

```bash
# 所有 check 开头的文件（约30个）
check_*.py

# 所有 test 开头的文件（约15个）  
test_*.py

# 所有 find 开头的文件（约10个）
find_*.py

# 其他一次性脚本
add_edges.py
analyze_exercises.py
analyze_exercises_fast.py
analyze_log.py
build_database.py
compare_two_coursewares.py
continue_analysis.py
delete_empty_analysis.py
delete_wrong_complex_review.py
diagnose_*.py
fix_lesson_case_titles.py
invest.py
load_exercise_analysis.py
merge.py
ppt_to_chroma.py
query_chroma.py
query_chroma2.py
quick_*.py
read_*.py
rebuild_*.py
remove_duplicates.py
run_test_and_save.py
search_complex_review_in_db.py
validate_exercise_analysis.py
verify_*.py
```

**建议操作**：
- ✅ 如果不再需要，直接删除
- ⚠️ 如果想保留参考，移动到 `scripts/archive/` 文件夹

---

## 🗑️ 第二类：根目录临时输出文件（约10个文件）

```
check_result.txt (16.5KB)
geometry_ggb_coord.txt (33.1KB)
kg_check_output.txt (105.5KB)
kg_gap_report.json (28.1KB)
kg_gap_report.txt (37.1KB)
resource_recommendation_results.json (10.2KB)
vector_db_ggb_coord.txt (9.3KB)
vector_search_result.txt (4.3KB)
测试结果 (632.2KB) ← 这个大文件特别占空间
check_kp_result.json (2.4KB)
```

**建议操作**：
- ✅ 全部删除（都是临时输出）

---

## 🗑️ 第三类：backend 目录调试脚本（约80个文件）

### backend/ 根目录下的调试脚本

```bash
# check 系列（约30个）
check_*.py

# test 系列（约25个）
test_*.py

# debug 系列（约8个）
debug_*.py

# fix 系列（约5个）
fix_*.py

# 其他一次性脚本
add_debug.py
add_debug2.py
add_merge_debug.py
build_index.py
comprehensive_test.py
count_placeholders.py
create_correct_cache.py
create_html.py
create_sanitized_cache.py
detect_encoding.py
find_*.py
force_refresh_cache.py
integrate_knowledge_graph.py
list_collections.py
list_lesson_plans.py
modify_file.py
optimize_retrieval.py
read_prob_csv.py
rebuild_and_verify.py
rebuild_db.py
rebuild_vector_db.py
run_local.py
search_monotonicity.py
simplify_separate.py
verify_*.py
```

**建议操作**：
- ✅ 大部分可以删除
- ⚠️ 保留重要的：
  - `run_local.py`（如果需要本地运行）
  - `comprehensive_test.py`（如果还在使用）

---

## 🗑️ 第四类：backend 临时输出文件

```
comprehensive_test_output.txt (8.3MB) ← 大文件
latest_lesson_plan.json (29.8KB)
geometry_with_suggestions.html (13.8KB)
ggb_complete.html (19.6KB)
feedback_dashboard.html (19.4KB)
```

**建议操作**：
- ✅ `comprehensive_test_output.txt` 可以删除（8.3MB）
- ⚠️ HTML 文件如果是演示用的，可以保留或移动到 `docs/` 目录
- ⚠️ `latest_lesson_plan.json` 如果是缓存，可以删除

---

## 🗑️ 第五类：过时的文档（可选删除）

### V41-V44 系列的旧文档

如果 V48 已经覆盖了这些功能，可以考虑删除或归档：

```
V41.6_文件名强制匹配改进总结.md
V41.7_标题字段检查改进总结.md
V41.8_多字段联合检查系统性改进总结.md
V41.9_多层次主题匹配系统性改进总结.md
V42.0_资源过滤调试日志增强系统性改进总结.md
V43.0_智能降级策略系统性改进总结.md
V43.1_智能降级策略优化总结.md
V43.2_Bug修复_state参数传递问题.md
V43.2_教学用途严格降级过滤总结.md
V44.0_SEARCH_AGENT_NODE提示词优化.md
```

### 可能重复的文档

```
V46.1_教案系统特殊指令意图识别修复-双重保障.md
V46.1_教案系统特殊指令意图识别修复.md
```

**建议操作**：
- ⚠️ 检查内容是否重复
- ✅ 如果重复，保留一个
- ⚠️ 如果想保留历史，移动到 `docs/archive/` 目录

---

## 🗑️ 第六类：空目录

```
exports/ (根目录，空)
logs/ (根目录，空)
test_chroma_db/ (根目录，空)
backend/logs/ (空)
backend/static/ (空)
```

**建议操作**：
- ✅ 可以删除（Git 会自动追踪空目录的问题）

---

## 🗑️ 第七类：__pycache__ 目录

```
backend/__pycache__/
```

**建议操作**：
- ✅ 可以删除（Python 会自动重新生成）
- ⚠️ 确保 `.gitignore` 中已包含 `__pycache__/`

---

## 📁 推荐的整理方案

### 方案1：激进清理（推荐用于生产环境）⭐

**删除所有调试脚本和临时文件**：

```powershell
# 在 PowerShell 中执行

# 1. 删除根目录的调试脚本
Remove-Item check_*.py
Remove-Item test_*.py
Remove-Item find_*.py
Remove-Item diagnose_*.py
Remove-Item quick_*.py
Remove-Item read_*.py
Remove-Item rebuild_*.py
Remove-Item verify_*.py
Remove-Item add_edges.py, analyze_*.py, build_database.py, ...

# 2. 删除临时输出文件
Remove-Item *.txt, *.json (除了 config.yaml, requirements.txt 等)
Remove-Item "测试结果"

# 3. 删除 backend 的调试脚本
cd backend
Remove-Item check_*.py, test_*.py, debug_*.py, fix_*.py, ...

# 4. 删除空目录
cd ..
Remove-Item exports, logs, test_chroma_db -Recurse
```

**预期效果**：
- 删除约 150+ 个文件
- 释放约 50-100 MB 空间
- 项目结构更清晰

---

### 方案2：保守整理（推荐用于开发环境）

**移动而不是删除**：

```powershell
# 1. 创建归档目录
New-Item -ItemType Directory -Path "scripts\archive" -Force
New-Item -ItemType Directory -Path "docs\archive" -Force

# 2. 移动调试脚本到 archive
Move-Item check_*.py scripts\archive\
Move-Item test_*.py scripts\archive\
Move-Item find_*.py scripts\archive\
# ... 其他脚本

# 3. 移动过时文档到 docs\archive
Move-Item V41*.md docs\archive\
Move-Item V42*.md docs\archive\
Move-Item V43*.md docs\archive\
Move-Item V44*.md docs\archive\

# 4. 删除真正的临时文件
Remove-Item *.txt, *.json (临时输出)
Remove-Item "测试结果"
```

**优点**：
- ✅ 保留了历史记录
- ✅ 可以随时恢复
- ✅ 项目结构仍然清晰

---

### 方案3：渐进清理（最安全）

**分批次清理，每次验证后再继续**：

**第一批**：临时输出文件（最安全）
```powershell
Remove-Item check_result.txt, kg_check_output.txt, ...
Remove-Item "测试结果"
```

**第二批**：明显的调试脚本
```powershell
Remove-Item check_*.py, test_*.py
```

**第三批**：其他脚本（谨慎）
```powershell
# 先查看有哪些
Get-ChildItem *.py | Where-Object { $_.Name -notmatch "^(main|start)" }

# 确认后再删除
```

---

## ⚠️ 重要提醒

### 删除前必须确认

1. **备份重要数据**：
   ```powershell
   # 创建备份
   Copy-Item . Mathemist_backup -Recurse
   ```

2. **确认不在使用中**：
   - 检查是否有脚本正在运行
   - 确认没有引用这些文件的代码

3. **检查 Git 状态**：
   ```bash
   git status
   ```
   确保没有未提交的更改

4. **更新 .gitignore**：
   确保以下模式已加入 `.gitignore`：
   ```gitignore
   # Python
   __pycache__/
   *.pyc
   *.pyo
   
   # 临时文件
   *.txt
   *.log
   test_*.py
   check_*.py
   debug_*.py
   
   # 临时输出
   comprehensive_test_output.txt
   latest_lesson_plan.json
   ```

---

## 🎯 我的建议

### 推荐操作顺序

1. **第一步**：删除临时输出文件（最安全）
   - `测试结果` (632KB)
   - `comprehensive_test_output.txt` (8.3MB)
   - 其他 `.txt`, `.json` 临时文件

2. **第二步**：删除空目录
   - `exports/`, `logs/`, `test_chroma_db/`

3. **第三步**：移动调试脚本到 archive（而不是删除）
   - 创建 `scripts/archive/`
   - 移动所有 `check_*.py`, `test_*.py`, `find_*.py` 等

4. **第四步**：观察一段时间
   - 如果没问题，再考虑永久删除

5. **第五步**：清理过时文档（可选）
   - 移动 V41-V44 文档到 `docs/archive/`

---

## 📝 快速清理命令（PowerShell）

```powershell
# ⚠️ 使用前请仔细阅读，确认无误后再执行

# 1. 删除临时输出文件
Remove-Item "测试结果", check_result.txt, kg_check_output.txt, vector_search_result.txt -ErrorAction SilentlyContinue
Remove-Item *.json -Exclude "knowledge_graph.json", "config.yaml" -ErrorAction SilentlyContinue

# 2. 删除空目录
Remove-Item exports, logs, test_chroma_db -Recurse -ErrorAction SilentlyContinue

# 3. 创建 archive 目录
New-Item -ItemType Directory -Path "scripts\archive" -Force | Out-Null

# 4. 移动调试脚本（不删除，只是移动）
Get-ChildItem check_*.py, test_*.py, find_*.py, diagnose_*.py -ErrorAction SilentlyContinue | Move-Item -Destination "scripts\archive\" -ErrorAction SilentlyContinue

Write-Host "✅ 清理完成！请检查 scripts\archive\ 目录确认文件已移动"
```

---

## 🔗 相关文档

- [.gitignore](file://d:\Git_Repository\Mathemist\.gitignore)
- [README.md](file://d:\Git_Repository\Mathemist\README.md)
