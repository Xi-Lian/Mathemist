# 检索解耦重构方案

## 1. 背景

当前项目的检索链路已经从“向量召回 + 少量规则增强”演变为“向量召回 + 大量规则补丁 + 多轮后处理”。  
短期内，这种方式可以修复个别查询 case；长期看，会带来以下问题：

- 检索、过滤、排序、分类、格式化混在同一个主流程中
- 大量 case-specific 规则堆积，行为越来越难预测
- 任何一个新需求都倾向于继续补规则，而不是调整整体架构
- 结果为什么被召回、为什么被过滤、为什么被排序到前面，越来越难解释
- 后续如果要引入 AI 二次筛选或更换召回策略，迁移成本很高

本方案的目标不是一次性重写全部检索逻辑，而是先把职责边界切开，再逐步把规则型判断迁移为“基础召回 + AI 筛选”的结构。

## 2. 当前主要问题

### 2.1 职责混杂

当前 `resource_retriever.py` 同时承担了这些职责：

- 查询理解
- 资源类型识别
- 查询增强
- 检索规划
- 向量召回
- 多主题拆分与合并
- 手工相关性打分
- 业务规则过滤
- 分类输出

这导致任何一个细节改动都会影响整体行为。

### 2.2 规则堆积

当前规则中存在大量以下特征：

- 针对单个主题的特判
- 针对单个题型的特判
- 针对单个资源类型的特判
- 提高 `n_results` 作为主要补救手段
- 用更多过滤规则去抵消扩大召回带来的噪声

这说明系统正在用规则模拟语义判断，而不是让语义判断模块承担主责。

### 2.3 排序逻辑不统一

目前存在多套排序和筛选依据：

- 向量距离
- 知识点匹配加分
- 资源类型加分
- 题型加分
- 年级加分
- 难度加分
- 通用概念优先
- 动态阈值
- 断层截断
- 多主题平衡

这些逻辑各自有合理性，但没有统一的排序中心，维护成本很高。

### 2.4 AI 的位置不对

目前系统主要用规则做语义判断，AI 只在上游或下游辅助。  
更合理的方式应当是：

- 程序负责收集候选
- AI 负责相关性筛选、适配度判断和最终排序

## 3. 重构目标

将当前检索系统改造成以下结构：

`用户请求 -> 查询理解 -> 基础召回 -> 候选整理 -> AI 筛选/重排 -> 结果分类输出`

目标原则：

- 规则数量显著下降
- 检索链路职责清晰
- 允许替换召回策略，而不影响筛选逻辑
- 允许替换 AI 筛选 prompt，而不影响底层数据访问
- 对外表现尽量稳定，迁移期可灰度

## 4. 目标架构

建议拆分为以下模块。

### 4.1 QueryUnderstanding

职责：

- 从用户输入中抽取结构化检索意图
- 识别主题、资源类型、题型、年级、难度、数量、教学场景
- 判断是否是多主题查询

输出建议：

```json
{
  "raw_query": "给我找高一函数单调性的证明题",
  "themes": ["函数的单调性"],
  "resource_types": ["习题"],
  "question_type": "证明题",
  "grade": "高一",
  "difficulty": null,
  "quantity_limit": 5,
  "user_constraints": {
    "explicit_resource_type": true,
    "explicit_grade": true
  }
}
```

### 4.2 RetrievalPlanner

职责：

- 根据结构化意图决定召回策略
- 决定单主题召回还是多主题召回
- 决定要查哪些资源类型
- 决定每种资源类型召回多少候选

注意：

- 这里不做复杂业务打分
- 不在这里堆 case-specific 规则
- 规划只负责“怎么取候选”，不负责“谁最相关”

### 4.3 CandidateRetriever

职责：

- 从存储层召回候选资源
- 支持向量召回
- 后续可以扩展为混合召回

建议保留的最薄程序过滤：

- 数据结构非法的资源剔除
- 资源去重
- 用户显式指定的资源类型约束

不建议继续在这一层做：

- 复杂主题特判
- 复杂题型推断
- 大量 relevance patch

### 4.4 CandidateAssembler

职责：

- 将多主题、多资源类型的候选结果合并
- 统一转换为 AI 可消费的候选摘要
- 控制传给 AI 的候选规模

输出建议：

```json
[
  {
    "candidate_id": "exercise_001",
    "resource_type": "exercise",
    "title": "函数单调性证明题 1",
    "summary": "高一，证明题，知识点包含函数的单调性",
    "metadata": {
      "grade": "高一",
      "question_type": "证明题",
      "knowledge_tags": ["函数的单调性"]
    }
  }
]
```

### 4.5 AIReranker

职责：

- 基于用户原始问题对候选进行筛选
- 判断候选是否真正符合需求
- 对候选重新排序
- 可以输出筛选理由

建议由 AI 处理的内容：

- 语义相关性
- 教学场景适配性
- 题型是否真正符合用户意图
- 难度是否合适
- 多资源之间的优先级判断

建议保留给程序的少量兜底：

- 用户明确说“只要课件/只要教案/只要习题”时，候选集合先做资源类型约束
- 空结果兜底
- 返回数量限制

### 4.6 ResultFormatter

职责：

- 将 AI 筛选结果映射回现有接口输出格式
- 按资源类型分类
- 填充前端需要的字段

## 5. 目标流程

### 5.1 新流程

1. 解析用户请求，得到结构化意图
2. 根据结构化意图规划召回
3. 召回固定规模候选，例如每类资源 10 到 20 条
4. 合并候选并生成摘要
5. 调用 AI 进行筛选和重排
6. 将 AI 选中的资源映射成现有输出结构

### 5.2 与旧流程的根本区别

旧流程：

- 规则主导
- 先大量召回
- 再多轮规则过滤
- AI 不在核心判断位

新流程：

- 候选召回主打稳定
- AI 主导语义筛选
- 规则只保留极少量兜底约束

## 6. 模块拆分建议

建议新增目录：

```text
backend/app/core/retrieval/
  __init__.py
  query_understanding.py
  retrieval_planner.py
  candidate_retriever.py
  candidate_assembler.py
  ai_reranker.py
  result_formatter.py
  schemas.py
```

建议保留旧文件，但逐步瘦身：

- `resource_retriever.py`
  第一阶段先作为兼容入口
  后续只保留 orchestrator 角色

- `query_preprocessor.py`
  可拆出纯文本清洗能力，供 `query_understanding.py` 使用

- `theme_matcher.py`
  不再直接主导最终排序
  可降级为候选特征提供器

## 7. 迁移策略

### 阶段 0：冻结继续堆规则

目标：

- 除线上 blocker 外，不再向旧检索链路增加新的特判规则
- 所有新增需求先进入方案评审，不直接补 if/else

### 阶段 1：抽离结构化意图层

动作：

- 新建 `query_understanding.py`
- 统一输出结构化检索请求对象
- 让旧 `resource_retriever.py` 先调用它

收益：

- 先把“理解用户要什么”和“怎么查”分开

### 阶段 2：抽离召回层

动作：

- 新建 `candidate_retriever.py`
- 把 Chroma 查询逻辑迁进去
- 统一单主题和多主题的召回接口

收益：

- 检索底层和业务后处理脱钩

### 阶段 3：引入 AI 二次筛选

动作：

- 新建 `ai_reranker.py`
- 输入：原始 query + 结构化意图 + 候选摘要
- 输出：保留列表、排序、筛选理由

收益：

- 用一次 AI 调用替代大量 case-specific 规则

### 阶段 4：保留兼容输出

动作：

- 新建 `result_formatter.py`
- 把 AI 结果映射成现有 `exercise_resources`、`lesson_plan_patterns` 等字段

收益：

- 前端和调用层不需要同步大改

### 阶段 5：清理旧规则

动作：

- 删除无效或重复规则
- 删除只服务于旧排序链路的加分项
- 缩减 `theme_matcher` 的职责

目标：

- 让 `resource_retriever.py` 从“大型规则文件”变成“协调器”

## 8. AI 筛选设计建议

### 8.1 输入建议

输入给 AI 的内容建议包含：

- 用户原始请求
- 结构化检索意图
- 候选资源摘要列表
- 每条候选的核心元数据

候选不要直接塞全文，建议提供：

- 标题
- 资源类型
- 简短摘要
- 年级
- 题型
- 知识点
- 来源路径

### 8.2 输出建议

建议 AI 输出结构化 JSON：

```json
{
  "selected_candidate_ids": ["exercise_001", "exercise_004"],
  "ranking": [
    {
      "candidate_id": "exercise_001",
      "score": 0.92,
      "reason": "与函数单调性证明题高度匹配，年级和题型均符合"
    }
  ],
  "rejected_candidate_ids": [
    {
      "candidate_id": "lesson_plan_003",
      "reason": "资源类型不符合用户当前需求"
    }
  ]
}
```

### 8.3 Prompt 原则

- 让 AI 判断“是否适合用户需求”，不是让 AI 自己重新生成资源
- 强调优先遵守用户明确要求
- 强调宁缺毋滥
- 强调按现有候选中最合适者排序，而不是脑补不存在的内容

## 9. 哪些规则可以删，哪些暂时保留

### 9.1 建议逐步删除

- 某个主题单独提高 `n_results`
- 某个题型单独提高 `n_results`
- 大量 query enhancement 同义词拼接
- 大量 relevance patch
- 多套动态阈值和断层规则
- 主题特判优先级规则

### 9.2 建议暂时保留

- 去重
- 用户显式指定的资源类型约束
- 返回数量控制
- 坏数据剔除
- 空结果兜底

## 10. 风险与控制

### 风险 1：AI 筛选不稳定

控制措施：

- 限定结构化输出
- 对候选做编号，避免模型自由发挥
- 初期保留程序兜底约束

### 风险 2：延迟上升

控制措施：

- 控制候选规模
- 每种资源类型只保留有限候选
- 候选只传摘要，不传全文

### 风险 3：效果短期波动

控制措施：

- 保留旧链路作为 fallback
- 做 A/B 或灰度
- 建立固定查询集回归测试

### 风险 4：旧代码继续膨胀

控制措施：

- 在迁移期间冻结新增规则
- 新需求优先评估是否应落到 AI 筛选层

## 11. 测试建议

建议建立固定测试查询集，覆盖以下场景：

- 单主题单资源类型
- 单主题多资源类型
- 多主题单资源类型
- 带年级约束
- 带题型约束
- 带难度约束
- 模糊表达
- 用户明确指定“只要某类资源”

建议记录指标：

- Top 3 命中率
- Top 5 命中率
- 误召回率
- 空结果率
- 平均响应时间

## 12. 推荐实施顺序

推荐优先级如下：

1. 先写结构化意图对象和新模块骨架
2. 把 Chroma 召回逻辑抽到独立模块
3. 接入 AI 二次筛选
4. 用旧接口格式兜底输出
5. 逐步删除旧规则

## 13. 最终预期

完成本次解耦后，理想状态应当是：

- 检索逻辑能被一句话解释清楚
- 新增需求优先改 prompt 或候选特征，而不是补特判
- `resource_retriever.py` 不再是上帝类
- 召回和筛选可以独立迭代
- 系统从“规则驱动检索”转向“候选驱动 + AI 判断”

## 14. 结论

当前项目最需要的不是继续补检索规则，而是先把检索架构解耦。  
重构方向建议明确转向：

- 基础召回尽量简单稳定
- 程序规则尽量薄
- AI 负责语义筛选和排序

本方案建议先从模块边界和迁移步骤入手，而不是直接全量重写。这样可以在保留现有系统可用性的前提下，逐步把检索系统从“补丁式增长”拉回可维护状态。
