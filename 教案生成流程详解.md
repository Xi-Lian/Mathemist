# 教案生成流程详解

## 📋 总体架构

Mathemist系统采用**统一教案生成系统**（UnifiedLessonPlanSystem），支持智能对话式教案生成。

## 🔄 完整流程

### 1. 入口层

#### API端点
- **主入口**: `/langgraph/math-agent/threads/{thread_id}/runs` (LangGraph流式接口)
- **直接生成**: `/math-agent/generate-lesson-plan` (绕过意图识别，直接生成)

#### 核心函数
```python
# backend/app/core/unified_lesson_plan/service.py
def generate_lesson_plan(user_input: str, session_id: Optional[str] = None) -> Dict[str, Any]
```

### 2. 请求处理层

**文件**: `backend/app/core/unified_lesson_plan/methods/process_lesson_plan_request.py`

#### 处理顺序（优先级从高到低）

1. **查看完整教案** (`handle_view_full_lesson_plan`)
   - 检测用户是否请求查看已生成的教案

2. **导出教案** (`handle_export_request`)
   - 检测用户是否请求导出教案（Markdown/HTML）

3. **格式选择回复** (`handle_format_selection_reply`)
   - 处理用户对导出格式的选择

4. **修改教案** (`handle_revision_request`)
   - 检测用户是否要求修改已生成的教案

5. **资源请求拒绝** (`should_reject_resource_request`)
   - 如果用户使用"推送"、"找"、"推荐"等资源检索指令词，拒绝生成教案

6. **准备生成轮次** (`prepare_generation_turn`)
   - 首次交互：提取信息，如果信息不足则生成欢迎语引导用户
   - 非首次交互：记录用户输入到对话历史

7. **直接生成判断** (`is_direct_generate_request`)
   - 如果用户明确要求直接生成，跳过引导流程

8. **生成流程处理** (`handle_generation_flow`)
   - 提取用户输入中的教案信息
   - 评估信息完整度
   - 根据完整度决定是继续引导还是生成教案

### 3. 信息收集层

#### 信息提取
**文件**: `backend/app/core/unified_lesson_plan/methods/extract_lesson_plan_info.py`

从用户输入中提取以下关键信息：
- **课题** (topic): 教案主题
- **教学目标** (objectives): 教学目标
- **授课对象** (audience): 学生年级/水平
- **课时安排** (duration): 课时数量
- **教学方法** (teaching_method): 教学策略
- **其他需求** (other_requirements): 额外要求

#### 信息完整度评估
**文件**: `backend/app/core/unified_lesson_plan/methods/assess_info_completion.py`

三个级别：
- **COMPLETE** (100%): 信息完整，可以生成教案
- **PARTIAL** (60%): 部分信息，需要补充
- **INSUFFICIENT** (30%): 信息不足，需要大量引导

### 4. 引导对话层

#### 信息不足时的引导
**文件**: `backend/app/core/unified_lesson_plan/methods/guide_for_more_info.py`

使用LLM生成引导性回复，询问缺失的关键信息。

#### 欢迎语生成
**文件**: `backend/app/core/unified_lesson_plan/process_helpers/generation_flow.py`

首次交互时，如果信息不足，生成欢迎语引导用户提供信息。

### 5. 资源检索层

**文件**: `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` (第32-47行)

#### 检索步骤

1. **构建增强输入**
   ```python
   enhanced_input = self._build_enhanced_input(collected_info)
   ```
   将收集的信息整合成完整的查询字符串

2. **调用资源检索器**
   ```python
   retrieved_resources = self.resource_retriever.retrieve(
       enhanced_input,
       intent="generate_lesson_plan"
   )
   ```

3. **获取三类资源**
   - **理论资源** (theory_resources): 数学理论卡片、知识点说明
   - **教案示例** (lesson_plan_patterns): 优秀教案模板和示例
   - **优秀案例分析** (excellent_case_resources): 教学案例和分析

4. **限制资源数量**
   - 理论资源：最多5条
   - 教案示例：最多3条
   - 优秀案例：最多3条

### 6. 教案生成层

**文件**: `backend/app/core/lesson_plan/generator.py`

#### 生成步骤

1. **分析教学方法**
   ```python
   teaching_method = self._analyze_teaching_method(user_input)
   ```

2. **分析内容类型**
   ```python
   content_type = self._analyze_content_type(user_input)
   ```

3. **格式化资源**
   ```python
   theory_text = self._format_theory_resources(theory_resources)
   patterns_text = self._format_lesson_plan_patterns(lesson_plan_patterns)
   excellent_case_text = self._format_excellent_case_resources(excellent_case_resources)
   ```

4. **调用LLM生成**
   ```python
   chain = self.prompt_template | model | StrOutputParser()
   lesson_plan = chain.invoke({
       "user_input": user_input,
       "theory_resources": theory_text,
       "lesson_plan_patterns": patterns_text,
       "excellent_case_resources": excellent_case_text,
       "lesson_plan_common_characteristics": self.lesson_plan_common_characteristics,
       "theory_cards": self.theory_cards
   })
   ```

5. **验证理论引用**
   ```python
   validated_lesson_plan = self._validate_theory_references(lesson_plan, teaching_method, content_type)
   ```

6. **质量评估**
   - 理论引用质量三维评估
   - 确保教案符合教学规范

### 7. 响应构建层

**文件**: `backend/app/core/response/methods/build_lesson_plan_response.py`

#### 响应内容

1. **教案摘要**
   - 生成教案的简要概述

2. **完整教案**
   - 存储在session中，可通过命令查看

3. **相关资源**
   - 展示用于生成教案的理论资源和示例

4. **操作提示**
   - 告知用户可以查看完整教案、修改或导出

### 8. 会话管理层

**文件**: `backend/app/core/unified_lesson_plan/methods/init.py`

#### 会话状态

```python
session = {
    "session_id": str,              # 会话ID
    "collected_info": dict,         # 收集的用户信息
    "conversation_history": list,   # 对话历史
    "lesson_plan": str,            # 生成的教案内容
    "progress": int,               # 进度百分比 (0-100)
    "last_activity": str,          # 最后活动时间戳
}
```

#### 会话超时清理
- 自动清理超过配置时间的过期会话
- 默认超时时间可在配置文件中设置

### 9. 持久化层

**文件**: `backend/app/core/unified_lesson_plan/methods/save_latest_lesson_plan.py`

#### 保存最新教案
- 保存到JSON文件：`backend/app/latest_lesson_plan.json`
- 包含教案内容和课题信息
- 便于快速访问最近生成的教案

## 📊 流程图

```
用户输入
  ↓
[1. 请求处理]
  ├─ 查看教案？ → 返回已生成的教案
  ├─ 导出教案？ → 执行导出
  ├─ 修改教案？ → 执行修改
  └─ 生成新教案？ ↓
  
[2. 信息提取]
  ↓
[3. 完整度评估]
  ├─ INSUFFICIENT → [4a. 引导补充信息] → 等待用户输入
  ├─ PARTIAL → [4b. 引导补充信息] → 等待用户输入
  └─ COMPLETE → [5. 资源检索] ↓
  
[5. 资源检索]
  ├─ 理论资源 (最多5条)
  ├─ 教案示例 (最多3条)
  └─ 优秀案例 (最多3条)
  ↓
[6. LLM生成教案]
  ├─ 分析教学方法
  ├─ 分析内容类型
  ├─ 格式化资源
  ├─ 调用LLM
  └─ 验证和优化
  ↓
[7. 保存和响应]
  ├─ 保存到session
  ├─ 保存到文件
  ├─ 生成摘要
  └─ 构建响应
  ↓
返回给用户
```

## 🔑 关键特性

### 1. 智能引导
- 不强制用户一次性提供所有信息
- 通过多轮对话逐步收集信息
- LLM生成自然的引导性问题

### 2. 资源驱动
- 基于向量数据库检索相关资源
- 理论资源提供知识支撑
- 教案示例提供结构参考
- 优秀案例提供实践指导

### 3. 会话管理
- 支持多轮对话上下文
- 自动清理过期会话
- 持久化最新教案

### 4. 灵活输出
- 支持查看完整教案
- 支持修改已有教案
- 支持导出为Markdown或HTML

### 5. 质量保证
- 理论引用验证
- 教学方法适配
- 内容类型匹配
- 三维质量评估

## 📁 核心文件清单

| 文件路径 | 功能 |
|---------|------|
| `backend/app/core/unified_lesson_plan/service.py` | 统一教案系统服务入口 |
| `backend/app/core/unified_lesson_plan/methods/process_lesson_plan_request.py` | 请求处理逻辑 |
| `backend/app/core/unified_lesson_plan/methods/extract_lesson_plan_info.py` | 信息提取 |
| `backend/app/core/unified_lesson_plan/methods/assess_info_completion.py` | 完整度评估 |
| `backend/app/core/unified_lesson_plan/methods/guide_for_more_info.py` | 引导对话 |
| `backend/app/core/unified_lesson_plan/methods/generate_complete_lesson_plan.py` | 完整教案生成 |
| `backend/app/core/unified_lesson_plan/methods/build_enhanced_input.py` | 构建增强输入 |
| `backend/app/core/lesson_plan/generator.py` | 教案生成器核心 |
| `backend/app/core/response/methods/build_lesson_plan_response.py` | 响应构建 |
| `backend/app/nodes.py` | LangGraph节点定义 |

## 🎯 使用示例

### 示例1：直接生成（信息完整）
```
用户：帮我生成一个高中数学必修一"函数的单调性"的教案，2课时，面向高一学生，采用探究式教学法
系统：🚀 检测到完整信息，直接生成教案
     [检索资源] → [LLM生成] → [返回教案]
```

### 示例2：引导式生成（信息不完整）
```
用户：我想做一个关于三角函数的教案
系统：好的，请告诉我：
     1. 具体的课题是什么？（如"正弦函数的图像与性质"）
     2. 授课对象是哪个年级？
     3. 计划安排几个课时？
     4. 有什么特殊的教学目标吗？

用户：课题是"正弦函数的图像与性质"，高一，2课时
系统：还缺少教学目标，您希望学生掌握什么？
     [继续引导...]

用户：让学生理解正弦函数的周期性，能画出图像
系统：🚀 信息已完整，开始生成教案
     [检索资源] → [LLM生成] → [返回教案]
```

### 示例3：修改教案
```
用户：把刚才的教案改成3课时
系统：[读取session中的教案] → [根据新要求修改] → [返回修改后的教案]
```

### 示例4：导出教案
```
用户：导出教案为HTML格式
系统：[读取session中的教案] → [转换为HTML] → [返回下载链接]
```

## 🔧 技术要点

### 1. Mixin模式
系统采用Mixin模式组织代码，每个功能模块独立：
- `_InitMixin`: 初始化
- `_ProcessLessonPlanRequestMixin`: 请求处理
- `_ExtractLessonPlanInfoMixin`: 信息提取
- `_GenerateCompleteLessonPlanMixin`: 教案生成
- 等等...

### 2. 会话状态管理
- 使用字典存储会话状态
- 支持多会话并发
- 自动超时清理

### 3. 资源检索集成
- 复用现有的ResourceRetriever
- 针对教案生成优化检索策略
- 限制资源数量保证聚焦

### 4. LLM提示工程
- 精心设计的提示模板
- 包含理论资源、教案示例、优秀案例
- 结合本地文件（优秀教案共性、理论卡片）

### 5. 质量控制
- 理论引用验证
- 教学方法适配检查
- 内容类型一致性检查
- 多维度质量评估
