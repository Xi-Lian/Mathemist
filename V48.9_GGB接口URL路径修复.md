# V48.9 GGB 接口 URL 路径修复

## 🐛 问题描述

**错误日志**：
```
INFO:     127.0.0.1:57578 - "POST /langgraph/math-agent/ggb/innovation-suggestions HTTP/1.1" 404 Not Found
```

**根本原因**：
- 前端调用 GGB 接口时，URL 变成了 `/langgraph/math-agent/ggb/innovation-suggestions`
- 但正确的路径应该是 `/ggb/innovation-suggestions`
- 问题出在 `NEXT_PUBLIC_API_URL` 环境变量的值包含了 `/langgraph/math-agent` 后缀

**环境变量配置**（`.env.local`）：
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/langgraph/math-agent
```

**前端代码**：
```tsx
const response = await fetch(
  `${apiBaseUrl}/ggb/innovation-suggestions`,  // apiBaseUrl = "http://localhost:8000/langgraph/math-agent"
  ...
);
// 最终 URL: http://localhost:8000/langgraph/math-agent/ggb/innovation-suggestions ❌
```

---

## ✅ 修复方案

### 修复：从 NEXT_PUBLIC_API_URL 中提取基础 URL

**文件**: `frontend/src/components/geometry/CombinedGeometryWorkbench.tsx`

**修改前**（第31-37行）：
```tsx
useEffect(() => {
  const initialApiFromQuery = searchParams.get("apiUrl")?.trim() ?? "";
  setApiBaseUrl(
    initialApiFromQuery || process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000",
  );
  setIsMounted(true);
}, [searchParams]);
```

**修改后**：
```tsx
useEffect(() => {
  const initialApiFromQuery = searchParams.get("apiUrl")?.trim() ?? "";
  // V48.9修复：GGB接口不使用 NEXT_PUBLIC_API_URL（它包含 /langgraph/math-agent）
  // 而是直接使用基础 URL
  if (initialApiFromQuery) {
    setApiBaseUrl(initialApiFromQuery);
  } else {
    // 从 NEXT_PUBLIC_API_URL 中提取基础 URL（去掉 /langgraph/math-agent）
    const envUrl = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
    const baseUrl = envUrl.replace(/\/langgraph\/math-agent$/, "");
    setApiBaseUrl(baseUrl);
  }
  setIsMounted(true);
}, [searchParams]);
```

**关键改进**：
- ✅ 检测并移除 `/langgraph/math-agent` 后缀
- ✅ 使用正则表达式精确匹配
- ✅ 保留用户自定义的 apiUrl 查询参数

---

## 📊 技术细节

### URL 处理逻辑

**输入**：
```
NEXT_PUBLIC_API_URL = "http://localhost:8000/langgraph/math-agent"
```

**处理**：
```tsx
const envUrl = process.env.NEXT_PUBLIC_API_URL;  // "http://localhost:8000/langgraph/math-agent"
const baseUrl = envUrl.replace(/\/langgraph\/math-agent$/, "");  // "http://localhost:8000"
```

**输出**：
```
apiBaseUrl = "http://localhost:8000"
最终请求 URL = "http://localhost:8000/ggb/innovation-suggestions" ✅
```

---

### 正则表达式说明

```javascript
/\/langgraph\/math-agent$/
```

- `\/`：转义的正斜杠 `/`
- `langgraph`：匹配字符串 "langgraph"
- `\/`：转义的正斜杠 `/`
- `math-agent`：匹配字符串 "math-agent"
- `$`：锚定到字符串末尾

**匹配示例**：
- ✅ `"http://localhost:8000/langgraph/math-agent"` → 匹配
- ❌ `"http://localhost:8000/langgraph/math-agent/other"` → 不匹配（因为有额外路径）
- ❌ `"http://localhost:8000"` → 不匹配（没有后缀）

---

## 🎯 不同接口的 URL 需求

### LangGraph Agent 接口

**需要完整路径**：
```
POST /langgraph/math-agent/stream
POST /langgraph/math-agent/invoke
```

**使用**：
```tsx
const apiUrl = process.env.NEXT_PUBLIC_API_URL;  // "http://localhost:8000/langgraph/math-agent"
```

---

### GGB 设计建议接口

**只需要基础 URL**：
```
POST /ggb/innovation-suggestions
```

**使用**：
```tsx
const baseUrl = process.env.NEXT_PUBLIC_API_URL.replace(/\/langgraph\/math-agent$/, "");
// "http://localhost:8000"
```

---

### 其他自定义接口

**根据实际需求决定**：
- 如果接口在根路径：使用基础 URL
- 如果接口在特定路径：拼接完整路径

---

## 🧪 测试方法

### 1. 检查 apiBaseUrl 的值

在前端控制台添加调试日志：

```tsx
useEffect(() => {
  console.log("NEXT_PUBLIC_API_URL:", process.env.NEXT_PUBLIC_API_URL);
  console.log("apiBaseUrl:", apiBaseUrl);
}, [apiBaseUrl]);
```

**预期输出**：
```
NEXT_PUBLIC_API_URL: http://localhost:8000/langgraph/math-agent
apiBaseUrl: http://localhost:8000
```

---

### 2. 检查网络请求

打开浏览器开发者工具 → Network 标签：

**请求 URL**：
```
POST http://localhost:8000/ggb/innovation-suggestions
```

**不应该看到**：
```
POST http://localhost:8000/langgraph/math-agent/ggb/innovation-suggestions  ❌
```

---

### 3. 测试接口调用

填写表单并点击"获取建议"：

**预期结果**：
- ✅ 请求成功（200 OK）
- ✅ 返回 Markdown 格式的建议
- ✅ 右侧显示建议内容

---

## 💡 更好的解决方案

### 方案1：分离环境变量（推荐）

在 `.env.local` 中添加专用变量：

```env
# LangGraph Agent API
NEXT_PUBLIC_API_URL=http://localhost:8000/langgraph/math-agent

# 基础 API URL（用于其他接口）
NEXT_PUBLIC_BASE_API_URL=http://localhost:8000
```

**使用**：
```tsx
// LangGraph 接口
const agentUrl = process.env.NEXT_PUBLIC_API_URL;

// GGB 接口
const ggbUrl = process.env.NEXT_PUBLIC_BASE_API_URL;
```

---

### 方案2：创建 URL 工具函数

```tsx
// utils/api.ts
export function getBaseUrl(): string {
  const fullUrl = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000/langgraph/math-agent";
  return fullUrl.replace(/\/langgraph\/math-agent$/, "");
}

export function getAgentUrl(): string {
  return process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000/langgraph/math-agent";
}
```

**使用**：
```tsx
import { getBaseUrl } from "@/utils/api";

const response = await fetch(
  `${getBaseUrl()}/ggb/innovation-suggestions`,
  ...
);
```

---

### 方案3：使用相对路径

如果前端和后端在同一域名下：

```tsx
const response = await fetch(
  "/ggb/innovation-suggestions",  // 相对路径
  ...
);
```

**优点**：
- 不需要关心基础 URL
- 自动适配开发和生产环境

**缺点**：
- 需要配置代理（开发环境）
- 跨域时需要额外配置

---

## 🚀 后续优化建议

### 1. 添加 URL 验证

```tsx
function validateApiUrl(url: string): boolean {
  try {
    new URL(url);
    return true;
  } catch {
    return false;
  }
}

if (!validateApiUrl(apiBaseUrl)) {
  setErrorText("无效的 API 地址");
  return;
}
```

---

### 2. 添加重试机制

```tsx
async function fetchWithRetry(url: string, options: RequestInit, retries = 3) {
  for (let i = 0; i < retries; i++) {
    try {
      const response = await fetch(url, options);
      if (response.ok) return response;
    } catch (error) {
      if (i === retries - 1) throw error;
      await new Promise(resolve => setTimeout(resolve, 1000 * (i + 1)));
    }
  }
}
```

---

### 3. 添加请求超时

```tsx
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 10000); // 10秒超时

try {
  const response = await fetch(url, {
    ...options,
    signal: controller.signal,
  });
  clearTimeout(timeoutId);
  return response;
} catch (error) {
  clearTimeout(timeoutId);
  if (error.name === 'AbortError') {
    throw new Error("请求超时");
  }
  throw error;
}
```

---

## 📋 总结

### 已完成
- ✅ 从 NEXT_PUBLIC_API_URL 中提取基础 URL
- ✅ 移除 `/langgraph/math-agent` 后缀
- ✅ 确保 GGB 接口使用正确的路径

### 核心价值
- 🎯 **准确性**：GGB 接口调用正确的 URL
- 🎯 **灵活性**：支持用户自定义 apiUrl
- 🎯 **兼容性**：不影响其他 LangGraph 接口

### 预期效果
- 404 错误：✅ 已解决
- 接口调用：正常工作
- 用户体验：流畅无错误

这个修复让 GGB 设计建议功能**能够正确调用后端接口**！🎉
