# DeepSeek 大模型本地部署指南

## 📋 目录

1. [为什么本地部署](#为什么本地部署)
2. [方案选择](#方案选择)
3. [快速部署](#快速部署)
4. [详细步骤](#详细步骤)
5. [性能优化](#性能优化)
6. [常见问题](#常见问题)

---

## 💡 为什么本地部署

### 云端 API vs 本地部署对比

| 特性 | 云端 API | 本地部署 |
|------|---------|---------|
| **响应速度** | 慢（2-10秒） | ⚡ 快（0.5-2秒） |
| **网络延迟** | 高（受网络影响） | ✅ 无（本地调用） |
| **成本** | 按调用付费 | ✅ 一次性投入 |
| **稳定性** | 依赖外部服务 | ✅ 完全可控 |
| **隐私性** | 数据传到云端 | ✅ 数据不出服务器 |
| **并发限制** | 受API限制 | ✅ 取决于GPU |
| **部署难度** | 简单 | 中等 |

### 🎯 推荐本地部署的原因

1. ✅ **极速响应**：从 2-10秒 降低到 0.5-2秒
2. ✅ **成本低**：无需支付 API 费用
3. ✅ **高可用**：不依赖外部服务
4. ✅ **隐私安全**：数据完全在本地
5. ✅ **充分利用 GPU**：16GB 显存足够运行 7B/8B 模型

---

## 🛠️ 方案选择

根据你的服务器配置（GPU 16GB），推荐以下方案：

### 方案1：Ollama（⭐ 强烈推荐）

**优势**：
- ✅ 安装简单，一键部署
- ✅ 自动管理模型下载和加载
- ✅ 支持多种模型（DeepSeek、Llama、Qwen等）
- ✅ 社区活跃，文档完善
- ✅ RESTful API，易于集成

**适用场景**：
- 中小规模应用
- 快速原型开发
- 需要灵活切换模型

**资源需求**：
- DeepSeek-R1 7B: ~8GB 显存
- DeepSeek-R1 8B: ~10GB 显存

---

### 方案2：vLLM（高性能）

**优势**：
- ✅ 推理速度极快（比 Ollama 快 2-3倍）
- ✅ 支持高并发
- ✅ 内存优化好
- ✅ OpenAI 兼容 API

**劣势**：
- ❌ 配置复杂
- ❌ 需要手动管理模型

**适用场景**：
- 高并发场景（>10 QPS）
- 对延迟要求极高

**资源需求**：
- DeepSeek-R1 7B: ~6GB 显存（量化后）

---

### 方案3：HuggingFace Transformers（灵活）

**优势**：
- ✅ 最灵活，可自定义
- ✅ 支持所有 HuggingFace 模型

**劣势**：
- ❌ 需要自己编写服务代码
- ❌ 性能不如 vLLM

**适用场景**：
- 需要高度定制
- 研究用途

---

## 🚀 快速部署（推荐 Ollama）

### 第一步：上传脚本到服务器

```bash
# 在你的本地机器上执行
scp deploy_llm_local.sh root@49.233.140.116:/tmp/
```

### 第二步：SSH 连接服务器

```bash
ssh root@49.233.140.116
```

### 第三步：执行部署脚本

```bash
cd /tmp
chmod +x deploy_llm_local.sh
./deploy_llm_local.sh
```

脚本会自动：
1. ✅ 检查 GPU 环境
2. ✅ 安装 Ollama
3. ✅ 下载 DeepSeek-R1 7B 模型
4. ✅ 启动服务
5. ✅ 配置远程访问
6. ✅ 生成 .env.local 配置文件

---

## 📖 详细步骤（手动部署）

如果自动脚本失败，可以手动执行以下步骤：

### 1. 安装 Ollama

```bash
# Ubuntu/Debian
curl -fsSL https://ollama.com/install.sh | sh

# CentOS/RHEL
sudo yum install -y ollama
```

### 2. 启动 Ollama 服务

```bash
# 后台启动
ollama serve &

# 或者使用 systemd（推荐）
sudo systemctl enable ollama
sudo systemctl start ollama
```

### 3. 下载 DeepSeek 模型

```bash
# 推荐：DeepSeek-R1 7B（平衡性能和速度）
ollama pull deepseek-r1:7b

# 其他选项：
# ollama pull deepseek-r1:1.5b  # 超轻量
# ollama pull deepseek-r1:8b    # 高质量
```

**下载时间**：约 10-30 分钟（取决于网速）

### 4. 测试模型

```bash
# 命令行测试
ollama run deepseek-r1:7b "你好"

# API 测试
curl http://localhost:11434/api/generate \
  -d '{"model": "deepseek-r1:7b", "prompt": "你好"}'
```

### 5. 配置远程访问

编辑 Ollama 服务配置：

```bash
# 创建 systemd 覆盖配置
sudo mkdir -p /etc/systemd/system/ollama.service.d
sudo tee /etc/systemd/system/ollama.service.d/environment.conf << EOF
[Service]
Environment="OLLAMA_HOST=0.0.0.0:11434"
EOF

# 重启服务
sudo systemctl daemon-reload
sudo systemctl restart ollama
```

### 6. 防火墙配置

```bash
# Ubuntu (ufw)
sudo ufw allow 11434/tcp

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-port=11434/tcp
sudo firewall-cmd --reload
```

---

## 🔧 配置后端使用本地模型

### 1. 创建 .env.local 文件

在 `backend/` 目录下创建 `.env.local`：

```env
# 大模型配置
USE_LOCAL_LLM=true
LOCAL_LLM_URL=http://localhost:11434
LOCAL_LLM_ENDPOINT=/api/generate
LOCAL_LLM_MODEL=deepseek-r1:7b

# 如果需要使用云端 API，取消下面的注释
# DEEPSEEK_API_KEY=your_api_key_here
```

### 2. 更新 LLM 客户端代码

修改 `backend/app/core/llm_client.py`（或类似文件）：

```python
import os
import requests

class LLMClient:
    def __init__(self):
        self.use_local = os.getenv("USE_LOCAL_LLM", "false").lower() == "true"
        
        if self.use_local:
            self.base_url = os.getenv("LOCAL_LLM_URL", "http://localhost:11434")
            self.endpoint = os.getenv("LOCAL_LLM_ENDPOINT", "/api/generate")
            self.model = os.getenv("LOCAL_LLM_MODEL", "deepseek-r1:7b")
        else:
            # 使用云端 API
            self.api_key = os.getenv("DEEPSEEK_API_KEY")
    
    def generate(self, prompt: str, **kwargs) -> str:
        if self.use_local:
            return self._local_generate(prompt, **kwargs)
        else:
            return self._cloud_generate(prompt, **kwargs)
    
    def _local_generate(self, prompt: str, **kwargs) -> str:
        """使用本地 Ollama 模型生成"""
        url = f"{self.base_url}{self.endpoint}"
        
        payload = {
            "model": self.model,
            "prompt": prompt,
            "stream": False,
            **kwargs
        }
        
        response = requests.post(url, json=payload, timeout=60)
        response.raise_for_status()
        
        result = response.json()
        return result.get("response", "")
    
    def _cloud_generate(self, prompt: str, **kwargs) -> str:
        """使用云端 API 生成"""
        # 原有的云端 API 调用逻辑
        pass
```

### 3. 重启后端服务

```bash
cd backend
python main.py
```

---

## ⚡ 性能优化

### 1. 模型量化（减少显存占用）

```bash
# 使用 4-bit 量化版本（推荐）
ollama pull deepseek-r1:7b-q4_K_M

# 其他量化选项：
# q2_K: 最小，质量略低
# q4_K_M: 平衡（推荐）
# q8_0: 最高质量，显存占用大
```

### 2. 调整并发参数

编辑 Ollama 配置：

```bash
sudo tee /etc/systemd/system/ollama.service.d/limits.conf << EOF
[Service]
LimitNOFILE=65536
LimitNPROC=65536
EOF

sudo systemctl daemon-reload
sudo systemctl restart ollama
```

### 3. GPU 内存优化

```bash
# 设置 GPU 内存利用率（vLLM）
--gpu-memory-utilization 0.9

# 或者限制最大上下文长度
--max-model-len 4096
```

### 4. 缓存优化

```python
# 在 LLMClient 中添加缓存
from functools import lru_cache

class LLMClient:
    @lru_cache(maxsize=1000)
    def generate_cached(self, prompt: str) -> str:
        return self.generate(prompt)
```

---

## 📊 性能对比

### 预期性能提升

| 指标 | 云端 API | 本地部署（Ollama） | 提升 |
|------|---------|-------------------|------|
| **平均响应时间** | 2-10秒 | 0.5-2秒 | **5-10倍** |
| **P95 延迟** | 8-15秒 | 2-4秒 | **4-6倍** |
| **并发能力** | 受限 | 5-10 QPS | **无限制** |
| **月度成本** | ¥500-2000 | ¥0 | **节省100%** |

### 实际测试

```bash
# 测试脚本
for i in {1..10}; do
    time curl -s http://localhost:11434/api/generate \
      -d '{"model": "deepseek-r1:7b", "prompt": "解释勾股定理"}' \
      | jq '.response' | wc -c
done
```

---

## ❓ 常见问题

### Q1: 显存不足怎么办？

**解决方案**：
1. 使用更小的模型（1.5B 或 7B 量化版）
2. 关闭其他 GPU 应用
3. 调整 batch size

```bash
# 检查显存使用
nvidia-smi

# 使用量化模型
ollama pull deepseek-r1:7b-q4_K_M
```

### Q2: 响应速度慢？

**排查步骤**：
1. 检查 GPU 是否被正确使用：`nvidia-smi`
2. 检查是否有其他进程占用 GPU
3. 尝试更小的模型

```bash
# 监控 GPU 使用
watch -n 1 nvidia-smi
```

### Q3: 如何更新模型？

```bash
# 拉取最新版本
ollama pull deepseek-r1:7b

# 删除旧版本
ollama rm deepseek-r1:7b-old
```

### Q4: 如何实现高可用？

**方案**：
1. 使用 systemd 自动重启
2. 添加健康检查
3. 多实例负载均衡（需要更多 GPU）

```bash
# 健康检查脚本
#!/bin/bash
if curl -s http://localhost:11434/api/tags > /dev/null; then
    echo "✅ Ollama 服务正常"
else
    echo "❌ Ollama 服务异常，重启..."
    sudo systemctl restart ollama
fi
```

### Q5: 如何备份模型？

```bash
# 导出模型
ollama cp deepseek-r1:7b deepseek-r1:7b-backup

# 备份模型文件
sudo tar czf ollama-models.tar.gz /root/.ollama/models/
```

---

## 🔗 相关资源

- [Ollama 官方文档](https://ollama.com/docs)
- [DeepSeek 模型列表](https://ollama.com/library/deepseek-r1)
- [vLLM 文档](https://docs.vllm.ai/)
- [HuggingFace Transformers](https://huggingface.co/docs/transformers)

---

## 📞 技术支持

如遇到问题，请检查：
1. GPU 驱动是否正常：`nvidia-smi`
2. Ollama 服务是否运行：`systemctl status ollama`
3. 端口是否开放：`netstat -tlnp | grep 11434`
4. 日志信息：`journalctl -u ollama -f`

---

**祝你部署顺利！🚀**
