# 资源库扩展指南：添加其他知识板块资源

## 问题描述

当前系统在查询非函数板块的内容时会返回"未找到资源"，这是因为：
1. 资源库中只有函数板块的资源
2. 系统添加了拦截逻辑，当识别到非函数主题时直接返回空结果

当你在资源库中添加其他板块资源时，需要确保系统能正确处理这些查询。

## 解决方案

### 步骤1：更新 knowledge_hierarchy

在 `backend/app/core/resource_retriever.py` 文件中，找到 `knowledge_hierarchy` 字典（大约在 1846 行开始），为新的知识板块添加条目。

**示例：添加概率板块**

```python
knowledge_hierarchy = {
    # 现有的函数相关主题...
    
    # 新增概率板块
    "概率": {
        "keywords": ["概率", "古典概型", "几何概型", "条件概率", "独立事件"],
        "chapters": ["第六章-概率", "第六章"]
    },
    "统计": {
        "keywords": ["统计", "数据收集", "数据处理", "数据分析", "抽样方法"],
        "chapters": ["第六章-统计", "第六章"]
    },
    
    # 新增其他板块...
}
```

### 步骤2：更新 non_function_themes 列表

在 `retrieve` 函数中，找到 `all_non_function_themes` 列表（大约在 648 行开始），添加新的知识板块：

```python
all_non_function_themes = [
    "立体几何", "空间点线面", "空间几何体", 
    "概率", "统计", "概率与统计",
    "数列", "不等式", "圆锥曲线", "导数",
    # 新增的板块...
]
```

### 步骤3：添加新板块的关键词检测（可选）

如果新板块的主题不在 `knowledge_hierarchy` 中，在 `_extract_theme_with_keywords` 函数的 `other_knowledge_areas` 字典中添加它：

```python
other_knowledge_areas = {
    "数列": ["数列", "等差", "等比", "通项", "求和"],
    "不等式": ["不等式", "不等式组", "线性规划", "均值不等式"],
    "圆锥曲线": ["圆锥曲线", "椭圆", "双曲线", "抛物线"],
    "导数": ["导数", "微分", "切线", "极值", "最值", "单调性"],
    # 新增的板块...
}
```

### 步骤4：测试新板块的查询

添加完资源和配置后，测试新板块的查询是否能正确返回资源：

```python
# 测试代码示例
from backend.app.core.intent_analyzer import IntentAnalyzer
from backend.app.core.resource_retriever import ResourceRetriever

def test_new_topic(query):
    analyzer = IntentAnalyzer()
    intent_result = analyzer.analyze(query)
    
    retriever = ResourceRetriever()
    result = retriever.retrieve(
        query=query,
        intent=intent_result.get('intent'),
        resource_types=intent_result.get('resource_types'),
        quantity_limit=intent_result.get('quantity_limit'),
        grade_info=intent_result.get('grade_info'),
        difficulty_info=intent_result.get('difficulty_info'),
        clarified_topic=intent_result.get('clarified_topic')
    )
    
    total_resources = 0
    for key, value in result.items():
        if key != 'hidden_resources' and isinstance(value, list):
            total_resources += len(value)
    
    print(f"查询: {query}")
    print(f"找到资源数: {total_resources}")
    return total_resources

# 测试新板块的查询
test_new_topic("给我一些概率的练习题")
test_new_topic("有没有统计的题目？")
```

## 关键原理

当前的拦截逻辑是：
1. 检查主题是否在 `knowledge_hierarchy` 中
2. 检查主题是否包含"函数"
3. 如果主题在 `knowledge_hierarchy` 中但不包含"函数"，则返回空结果

**当你添加了新板块的资源后**：
1. 系统会从 `knowledge_hierarchy` 中找到这些新主题
2. 拦截逻辑会检查这些主题是否包含"函数"
3. 由于这些主题是新板块的，不包含"函数"，所以仍然会被拦截

## 如何让新板块的资源正常返回

当你真正添加了新板块的资源后，需要：

### 方法1：移除拦截逻辑

在 `retrieve` 函数中，找到拦截逻辑代码并注释掉：

```python
# # V53.4改进：如果识别到的主题在knowledge_hierarchy中，但不是函数相关的主题，直接返回空结果
# if core_theme:
#     theme_list = [t.strip() for t in core_theme.split(',') if t.strip()]
#     all_non_function_themes = [
#         "立体几何", "空间点线面", "空间几何体", 
#         "概率", "统计", "概率与统计",
#         "数列", "不等式", "圆锥曲线", "导数"
#     ]
#     
#     # 检查是否有任何非函数主题
#     has_non_function_theme = False
#     for theme in theme_list:
#         if theme in all_non_function_themes:
#             has_non_function_theme = True
#             break
#         # 也检查主题是否在knowledge_hierarchy中但不包含"函数"
#         if theme in self.knowledge_hierarchy and "函数" not in theme:
#             has_non_function_theme = True
#             break
#     
#     if has_non_function_theme:
#         print(f"⚠️ V53.4检测到非函数主题查询，直接返回空结果（资源库只有函数板块）")
#         return self._get_empty_result()
```

### 方法2：修改拦截逻辑

修改拦截逻辑，让它只拦截那些真正没有资源的板块：

```python
# V53.4改进：如果识别到的主题在knowledge_hierarchy中，但不是函数相关的主题，检查是否有资源
if core_theme:
    theme_list = [t.strip() for t in core_theme.split(',') if t.strip()]
    
    # 检查是否有任何非函数主题
    has_non_function_theme = False
    for theme in theme_list:
        # 检查主题是否在knowledge_hierarchy中但不包含"函数"
        if theme in self.knowledge_hierarchy and "函数" not in theme:
            has_non_function_theme = True
            break
    
    if has_non_function_theme:
        # 这里可以添加逻辑来检查是否有该主题的资源
        # 如果没有资源，返回空结果
        # 如果有资源，继续处理
        print(f"⚠️ V53.4检测到非函数主题查询，继续处理")
        # 不再直接返回空结果
```

## 注意事项

1. **资源文件组织**：确保新板块的资源文件按照与函数板块相同的结构组织
2. **元数据标记**：为新板块的资源添加正确的知识点标签
3. **向量数据库重建**：添加新资源后，需要重新构建向量数据库
4. **测试**：添加新资源后，一定要测试相关的查询

## 示例：添加概率板块的完整流程

1. **创建资源文件**：在 `learning_resource/习题/` 目录下创建 `概率` 文件夹，添加相关习题
2. **更新 knowledge_hierarchy**：添加概率相关主题
3. **更新 non_function_themes**：添加"概率"和"统计"
4. **测试查询**：测试"给我一些概率的练习题"等查询
5. **重建向量数据库**：确保新资源被索引
6. **验证结果**：确认查询能返回正确的资源

## 总结

当前的拦截逻辑是为了在资源库只有函数板块时，让其他板块的查询正确返回"未找到资源"。当你添加了其他板块的资源后，系统可以通过更新 `knowledge_hierarchy` 来自动支持这些新板块，无需修改核心逻辑。

通过以上步骤，你可以轻松扩展资源库，添加新的知识板块，并确保系统能正确处理这些板块的查询。