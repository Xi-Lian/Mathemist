# Mathemist 上线部署检查清单

## 📋 部署前准备

### ✅ 1. 服务器环境检查

- [ ] SSH 可以连接到服务器：`ssh root@49.233.140.116`
- [ ] GPU 驱动已安装：`nvidia-smi` 显示正常
- [ ] CUDA 已安装：`nvcc --version` 显示版本
- [ ] 防火墙已配置：开放 80、443、22 端口
- [ ] 磁盘空间充足：`df -h` 至少 50GB 可用

### ✅ 2. 代码准备

- [ ] 最新代码已推送到 Git
- [ ] 清理了调试脚本（已完成 ✅）
- [ ] 配置文件已准备好：
  - [ ] `backend/.env` - 生产环境配置
  - [ ] `backend/config.yaml` - 应用配置
  - [ ] `frontend/.env.local` - 前端配置

### ✅ 3. 资源文件准备

- [ ] ChromaDB 向量数据库已构建
- [ ] learning_resource 资源文件完整
- [ ] GGB 信息表 Excel 文件存在
- [ ] 教学大纲 Markdown 文件存在
- [ ] knowledge_graph.json 知识图谱数据

---

## 🚀 部署步骤

### 第一步：部署 DeepSeek 大模型（本地）

**预计时间**：30-60 分钟（含模型下载）

```bash
# 1. 上传脚本
scp deploy_llm_local.sh root@49.233.140.116:/tmp/

# 2. SSH 连接
ssh root@49.233.140.116

# 3. 执行部署
chmod +x /tmp/deploy_llm_local.sh
bash /tmp/deploy_llm_local.sh
```

**验证**：
- [ ] Ollama 服务运行：`systemctl status ollama`
- [ ] 模型已下载：`ollama list` 显示 deepseek-r1:7b
- [ ] API 可访问：`curl http://localhost:11434/api/tags`
- [ ] 测试生成：`ollama run deepseek-r1:7b "你好"`

**预期结果**：
```
✅ Ollama 服务运行在 http://localhost:11434
✅ DeepSeek-R1 7B 模型已下载
✅ .env.local 配置文件已生成
```

---

### 第二步：上传项目代码

**预计时间**：10-20 分钟（取决于网速）

#### 方法1：Git 克隆（推荐）

```bash
# 在服务器上执行
cd /opt
git clone <your-repo-url> mathemist
cd mathemist
```

#### 方法2：SCP 上传

```bash
# 在本地执行
scp -r backend root@49.233.140.116:/opt/mathemist/
scp -r frontend root@49.233.140.116:/opt/mathemist/
scp -r learning_resource root@49.233.140.116:/opt/mathemist/
```

---

### 第三步：配置后端环境

**预计时间**：5 分钟

```bash
# SSH 连接
ssh root@49.233.140.116

# 进入后端目录
cd /opt/mathemist/backend

# 创建 Python 虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 复制 .env.local（从大模型部署步骤生成）
cp /tmp/.env.local .

# 编辑 .env，配置生产环境变量
nano .env
```

**.env 配置示例**：

```env
# 服务器配置
HOST=0.0.0.0
PORT=8000

# 大模型配置（使用本地模型）
USE_LOCAL_LLM=true
LOCAL_LLM_URL=http://localhost:11434
LOCAL_LLM_ENDPOINT=/api/generate
LOCAL_LLM_MODEL=deepseek-r1:7b

# 数据库配置
CHROMA_DB_PATH=/opt/mathemist/chroma_db
VECTOR_DB_PATH=/opt/mathemist/vector_db

# 资源路径
RESOURCE_DIR=/opt/mathemist/learning_resource

# CORS 配置（允许所有域名访问）
CORS_ORIGINS=["*"]

# 日志配置
LOG_LEVEL=INFO
```

---

### 第四步：构建前端

**预计时间**：10-15 分钟

```bash
# 进入前端目录
cd /opt/mathemist/frontend

# 安装依赖
pnpm install

# 创建 .env.local
cat > .env.local << EOF
NEXT_PUBLIC_API_URL=http://49.233.140.116:8000
EOF

# 构建生产版本
pnpm build
```

**预期输出**：
```
✓ Compiled successfully
✓ Linting and checking validity of types
✓ Collecting page data
✓ Generating static pages
✓ Finalizing page optimization
```

---

### 第五步：配置 Nginx

**预计时间**：5 分钟

```bash
# 创建 Nginx 配置文件
sudo tee /etc/nginx/sites-available/mathemist << 'EOF'
server {
    listen 80;
    server_name 49.233.140.116;  # 如果有域名，改为你的域名

    # 前端静态文件
    location / {
        root /opt/mathemist/frontend/out;
        try_files $uri $uri/ /index.html;
        gzip on;
        gzip_types text/plain text/css application/json application/javascript;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
    }

    # GGB API 代理
    location /ggb/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

# 启用配置
sudo ln -s /etc/nginx/sites-available/mathemist /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

**验证**：
```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 测试访问
curl http://49.233.140.116
```

---

### 第六步：配置 systemd 服务

**预计时间**：5 分钟

#### 后端服务

```bash
# 创建 systemd 服务文件
sudo tee /etc/systemd/system/mathemist-backend.service << 'EOF'
[Unit]
Description=Mathemist Backend Service
After=network.target ollama.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mathemist/backend
Environment="PATH=/opt/mathemist/backend/venv/bin"
ExecStart=/opt/mathemist/backend/venv/bin/python main.py
Restart=always
RestartSec=10

# 日志
StandardOutput=append:/var/log/mathemist/backend.log
StandardError=append:/var/log/mathemist/backend-error.log

[Install]
WantedBy=multi-user.target
EOF

# 创建日志目录
sudo mkdir -p /var/log/mathemist

# 重载 systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl enable mathemist-backend
sudo systemctl start mathemist-backend

# 检查状态
sudo systemctl status mathemist-backend
```

#### 前端服务（可选，如果使用 Next.js SSR）

```bash
# 如果使用静态导出，不需要此步骤
# 如果需要 SSR，创建前端服务
sudo tee /etc/systemd/system/mathemist-frontend.service << 'EOF'
[Unit]
Description=Mathemist Frontend Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mathemist/frontend
ExecStart=/usr/bin/pnpm start
Restart=always

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl enable mathemist-frontend
sudo systemctl start mathemist-frontend
```

---

### 第七步：配置 SSL（HTTPS）

**预计时间**：10 分钟

**前提条件**：需要有域名并解析到 49.233.140.116

```bash
# 安装 Certbot
sudo apt-get install certbot python3-certbot-nginx

# 获取 SSL 证书
sudo certbot --nginx -d yourdomain.com

# 自动续期
sudo systemctl enable certbot.timer
```

**如果没有域名**：
- 可以暂时使用 HTTP
- 或使用自签名证书（浏览器会警告）

---

## ✅ 验证部署

### 1. 检查服务状态

```bash
# 检查所有服务
sudo systemctl status ollama
sudo systemctl status mathemist-backend
sudo systemctl status nginx

# 查看日志
sudo journalctl -u mathemist-backend -f
```

### 2. 测试 API

```bash
# 测试健康检查
curl http://49.233.140.116/api/health

# 测试 GGB 建议接口
curl -X POST http://49.233.140.116/ggb/innovation-suggestions \
  -H "Content-Type: application/json" \
  -d '{
    "chapter": "函数",
    "topic": "正弦函数",
    "teaching_purpose": "展示正弦函数的周期性"
  }'
```

**预期响应时间**：< 2秒（本地模型）

### 3. 测试前端

在浏览器中访问：
- `http://49.233.140.116` （HTTP）
- `https://yourdomain.com` （HTTPS，如果有域名）

**检查项**：
- [ ] 页面正常加载
- [ ] 可以输入章节和主题
- [ ] 点击"生成建议"按钮
- [ ] 2秒内返回结果
- [ ] 结果显示正确

### 4. 性能测试

```bash
# 并发测试（10个请求）
for i in {1..10}; do
  curl -s -o /dev/null -w "%{time_total}\n" \
    -X POST http://49.233.140.116/ggb/innovation-suggestions \
    -H "Content-Type: application/json" \
    -d '{"chapter":"函数","topic":"正弦函数","teaching_purpose":"测试"}' &
done
wait
```

**预期结果**：
- 平均响应时间：< 2秒
- P95 响应时间：< 4秒
- 无失败请求

---

## 🔧 故障排查

### 问题1：Ollama 服务未启动

```bash
# 检查状态
sudo systemctl status ollama

# 查看日志
sudo journalctl -u ollama -f

# 重启服务
sudo systemctl restart ollama
```

### 问题2：后端服务无法启动

```bash
# 查看日志
sudo journalctl -u mathemist-backend -f

# 常见错误：
# - 端口被占用：lsof -i:8000
# - 依赖缺失：pip install -r requirements.txt
# - 配置文件错误：检查 .env
```

### 问题3：Nginx 502 错误

```bash
# 检查后端是否运行
curl http://127.0.0.1:8000/api/health

# 检查 Nginx 配置
sudo nginx -t

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 问题4：响应速度慢

```bash
# 检查 GPU 使用
nvidia-smi

# 检查模型是否加载
ollama ps

# 查看后端日志
sudo journalctl -u mathemist-backend -f | grep "response_time"
```

---

## 📊 监控和维护

### 1. 设置日志轮转

```bash
# 创建 logrotate 配置
sudo tee /etc/logrotate.d/mathemist << 'EOF'
/var/log/mathemist/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 root root
}
EOF
```

### 2. 设置监控脚本

```bash
# 创建健康检查脚本
sudo tee /opt/mathemist/health_check.sh << 'EOF'
#!/bin/bash

# 检查 Ollama
if ! curl -s http://localhost:11434/api/tags > /dev/null; then
    echo "❌ Ollama 服务异常"
    sudo systemctl restart ollama
fi

# 检查后端
if ! curl -s http://127.0.0.1:8000/api/health > /dev/null; then
    echo "❌ 后端服务异常"
    sudo systemctl restart mathemist-backend
fi

# 检查 Nginx
if ! curl -s http://localhost > /dev/null; then
    echo "❌ Nginx 服务异常"
    sudo systemctl restart nginx
fi

echo "✅ 所有服务正常"
EOF

chmod +x /opt/mathemist/health_check.sh

# 添加到 crontab（每5分钟检查一次）
(crontab -l 2>/dev/null; echo "*/5 * * * * /opt/mathemist/health_check.sh >> /var/log/mathemist/health.log 2>&1") | crontab -
```

### 3. 备份策略

```bash
# 创建备份脚本
sudo tee /opt/mathemist/backup.sh << 'EOF'
#!/bin/bash

BACKUP_DIR="/opt/backups/mathemist/$(date +%Y%m%d)"
mkdir -p $BACKUP_DIR

# 备份 ChromaDB
tar czf $BACKUP_DIR/chroma_db.tar.gz /opt/mathemist/chroma_db

# 备份向量数据库
tar czf $BACKUP_DIR/vector_db.tar.gz /opt/mathemist/vector_db

# 备份知识图谱
cp /opt/mathemist/knowledge_graph.json $BACKUP_DIR/

# 备份配置文件
cp /opt/mathemist/backend/.env $BACKUP_DIR/

# 删除7天前的备份
find /opt/backups/mathemist -type d -mtime +7 -exec rm -rf {} \;

echo "✅ 备份完成：$BACKUP_DIR"
EOF

chmod +x /opt/mathemist/backup.sh

# 每天凌晨2点备份
(crontab -l 2>/dev/null; echo "0 2 * * * /opt/mathemist/backup.sh") | crontab -
```

---

## 🎯 上线后优化

### 1. CDN 加速（可选）

- 使用 Cloudflare CDN 加速静态资源
- 配置缓存策略

### 2. 负载均衡（高并发场景）

- 使用多个后端实例
- Nginx 负载均衡配置

### 3. 数据库优化

- ChromaDB 索引优化
- 定期清理无用数据

### 4. 模型优化

- 尝试更小的模型（1.5B）提升速度
- 使用量化版本减少显存占用

---

## 📞 技术支持

如遇到问题，请提供以下信息：

1. **服务状态**：
   ```bash
   sudo systemctl status ollama
   sudo systemctl status mathemist-backend
   sudo systemctl status nginx
   ```

2. **日志信息**：
   ```bash
   sudo journalctl -u mathemist-backend -n 100
   sudo tail -n 100 /var/log/nginx/error.log
   ```

3. **GPU 状态**：
   ```bash
   nvidia-smi
   ```

4. **网络测试**：
   ```bash
   curl -v http://49.233.140.116/api/health
   ```

---

## ✅ 上线确认清单

- [ ] 大模型本地部署完成
- [ ] 后端服务正常运行
- [ ] 前端页面可访问
- [ ] API 响应时间 < 2秒
- [ ] HTTPS 配置完成（可选）
- [ ] 监控脚本已设置
- [ ] 备份策略已配置
- [ ] 压力测试通过
- [ ] 文档已更新

**恭喜！系统已成功上线！🎉**
