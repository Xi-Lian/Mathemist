# Mathemist 生产环境部署指南

## 📋 目录

1. [服务器准备](#服务器准备)
2. [方案选择](#方案选择)
3. [快速部署](#快速部署)
4. [详细步骤](#详细步骤)
5. [配置优化](#配置优化)
6. [常见问题](#常见问题)

---

## 🖥️ 服务器准备

### 你的服务器配置

- **公网 IP**: 49.233.140.116
- **配置**: GPU 16GB, CPU 8核, 内存 32GB, 硬盘 200GB
- **系统**: Ubuntu/CentOS（推荐 Ubuntu 22.04）
- **带宽**: 峰值 10Mbps

### 前置要求

1. **SSH 访问**：确保可以通过 SSH 连接到服务器
   ```bash
   ssh root@49.233.140.116
   ```

2. **防火墙配置**：开放必要端口
   ```bash
   # Ubuntu (ufw)
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw allow 22/tcp
   sudo ufw enable
   
   # CentOS (firewalld)
   sudo firewall-cmd --permanent --add-service=http
   sudo firewall-cmd --permanent --add-service=https
   sudo firewall-cmd --permanent --add-service=ssh
   sudo firewall-cmd --reload
   ```

3. **域名（可选但推荐）**：
   - 购买域名并解析到 49.233.140.116
   - 例如：math.yourdomain.com → 49.233.140.116

---

## 💡 方案选择

### 方案对比

| 特性 | 方案A: 云端API | 方案B: 本地模型 |
|------|---------------|----------------|
| **响应速度** | 慢（网络延迟） | ⚡ 快（本地GPU） |
| **成本** | 按调用付费 | 一次性投入 |
| **稳定性** | 依赖网络 | 完全可控 |
| **并发能力** | 受API限制 | 取决于GPU |
| **部署难度** | 简单 | 中等 |
| **推荐场景** | 测试/低频使用 | **生产环境** ✅ |

### 🎯 推荐方案：方案B（本地部署大模型）

**原因**：
1. ✅ **极速响应**：无网络延迟，GPU 加速
2. ✅ **成本低**：无需支付 API 费用
3. ✅ **稳定性高**：不受外部服务影响
4. ✅ **隐私性好**：数据不出服务器

---

## 🚀 快速部署

### ⚠️ 重要：先部署大模型，再部署应用

**推荐部署顺序**：
1. **第一步**：部署 DeepSeek 大模型（本地）
2. **第二步**：部署 Mathemist 应用
3. **第三步**：配置 Nginx 反向代理

---

### 第一步：部署 DeepSeek 大模型

```bash
# 1. 上传大模型部署脚本
scp deploy_llm_local.sh root@49.233.140.116:/tmp/

# 2. SSH 连接
ssh root@49.233.140.116

# 3. 执行部署
chmod +x /tmp/deploy_llm_local.sh
bash /tmp/deploy_llm_local.sh
```

**详细说明**：请参考 [本地大模型部署指南.md](./本地大模型部署指南.md)

**预期结果**：
- ✅ Ollama 服务运行在 `http://localhost:11434`
- ✅ DeepSeek-R1 7B 模型已下载
- ✅ 生成 `.env.local` 配置文件

---

### 第二步：部署 Mathemist 应用

#### 方法1：一键部署脚本（推荐）

```bash
# 1. 上传部署脚本到服务器
scp deploy_production.sh root@49.233.140.116:/tmp/

# 2. SSH 连接到服务器
ssh root@49.233.140.116

# 3. 执行部署脚本
chmod +x /tmp/deploy_production.sh
bash /tmp/deploy_production.sh
```

脚本会自动完成：
- ✅ 安装系统依赖
- ✅ 配置 Python 环境
- ✅ 安装前端依赖
- ✅ 配置 Nginx
- ✅ 配置 systemd 服务

---

### 方法2：手动部署（适合自定义）

见下方[详细步骤](#详细步骤)

---

## 📝 详细步骤

### 步骤1：上传项目代码

在你的本地机器执行：

```bash
# 压缩项目（排除不必要的文件）
tar -czf mathemist.tar.gz \
  --exclude='node_modules' \
  --exclude='.next' \
  --exclude='__pycache__' \
  --exclude='*.pyc' \
  --exclude='chroma_db' \
  --exclude='vector_db' \
  backend/ frontend/ learning_resource/

# 上传到服务器
scp mathemist.tar.gz root@49.233.140.116:/opt/

# SSH 连接
ssh root@49.233.140.116

# 解压
cd /opt
mkdir -p mathemist
tar -xzf mathemist.tar.gz -C mathemist
```

---

### 步骤2：安装后端依赖

```bash
cd /opt/mathemist/backend

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

---

### 步骤3：配置环境变量

创建 `.env` 文件：

```bash
cat > .env << EOF
# ============================================
# 选项1：使用云端 DeepSeek API（较慢）
# ============================================
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-your-api-key-here
DEEPSEEK_MODEL=deepseek-chat

# ============================================
# 选项2：使用本地部署的大模型（推荐，快速）⭐
# ============================================
# LLM_PROVIDER=local
# LOCAL_MODEL_PATH=/opt/models/deepseek-r1
# LOCAL_MODEL_DEVICE=cuda

# 服务器配置
HOST=0.0.0.0
PORT=8000

# CORS 配置
CORS_ORIGINS=http://49.233.140.116,https://your-domain.com

# ChromaDB 路径
CHROMA_DB_PATH=/opt/mathemist/backend/chroma_db

# 日志级别
LOG_LEVEL=INFO

# 工作线程数（根据CPU核心数调整）
WORKERS=4
EOF
```

**编辑 API Key**：
```bash
nano .env
# 填入你的 DeepSeek API Key
```

---

### 步骤4：安装前端依赖

```bash
cd /opt/mathemist/frontend

# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装 pnpm
npm install -g pnpm

# 安装依赖
pnpm install

# 构建生产版本
pnpm build
```

---

### 步骤5：配置 Nginx

创建 Nginx 配置文件：

```bash
sudo tee /etc/nginx/sites-available/mathemist << 'EOF'
server {
    listen 80;
    server_name 49.233.140.116 your-domain.com;

    # 前端静态文件
    location / {
        root /opt/mathemist/frontend/out;
        try_files $uri $uri/ /index.html;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置（大模型推理可能需要较长时间）
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # LangGraph API
    location /langgraph/ {
        proxy_pass http://127.0.0.1:8000/langgraph/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # GGB API
    location /ggb/ {
        proxy_pass http://127.0.0.1:8000/ggb/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
EOF

# 启用站点
sudo ln -sf /etc/nginx/sites-available/mathemist /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

---

### 步骤6：配置 systemd 服务

创建后端服务文件：

```bash
sudo tee /etc/systemd/system/mathemist-backend.service << EOF
[Unit]
Description=Mathemist Backend Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mathemist/backend
Environment="PATH=/opt/mathemist/backend/venv/bin"
ExecStart=/opt/mathemist/backend/venv/bin/python main.py
Restart=always
RestartSec=10

# 资源限制
LimitNOFILE=65536

# 日志
StandardOutput=journal
StandardError=journal
SyslogIdentifier=mathemist-backend

[Install]
WantedBy=multi-user.target
EOF

# 重新加载 systemd
sudo systemctl daemon-reload

# 启用并启动服务
sudo systemctl enable mathemist-backend
sudo systemctl start mathemist-backend
```

---

### 步骤7：验证部署

```bash
# 检查服务状态
sudo systemctl status mathemist-backend
sudo systemctl status nginx

# 查看日志
sudo journalctl -u mathemist-backend -f

# 测试访问
curl http://49.233.140.116
curl http://49.233.140.116/api/info
```

在浏览器访问：`http://49.233.140.116`

---

## 🔒 启用 HTTPS（强烈推荐）

### 使用 Let's Encrypt 免费证书

```bash
# 安装 certbot
sudo apt-get install -y certbot python3-certbot-nginx

# 获取证书（如果有域名）
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo crontab -e
# 添加以下行：
# 0 0 1 * * certbot renew --quiet
```

---

## ⚡ 性能优化

### 1. 启用 Gzip 压缩

在 Nginx 配置中添加：

```nginx
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_proxied expired no-cache no-store private auth;
gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
```

### 2. 配置缓存

```nginx
# 静态文件缓存
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
}
```

### 3. 调整后端工作线程

在 `backend/main.py` 或启动命令中：

```bash
# 根据 CPU 核心数调整
WORKERS=8  # 8核CPU可以设置8个工作线程
```

---

## 📊 监控和维护

### 查看日志

```bash
# 后端日志
sudo journalctl -u mathemist-backend -f

# Nginx 访问日志
sudo tail -f /var/log/nginx/access.log

# Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 重启服务

```bash
# 重启后端
sudo systemctl restart mathemist-backend

# 重启 Nginx
sudo systemctl restart nginx
```

### 更新代码

```bash
# 停止服务
sudo systemctl stop mathemist-backend

# 备份当前版本
cp -r /opt/mathemist /opt/mathemist.backup

# 上传新版本代码
scp mathemist.tar.gz root@49.233.140.116:/opt/
ssh root@49.233.140.116 "cd /opt && rm -rf mathemist && mkdir mathemist && tar -xzf mathemist.tar.gz -C mathemist"

# 重新启动
sudo systemctl start mathemist-backend
```

---

## ❓ 常见问题

### Q1: 后端启动失败

**检查日志**：
```bash
sudo journalctl -u mathemist-backend -n 50
```

**常见原因**：
- 端口被占用：`sudo lsof -i :8000`
- 依赖未安装：`pip install -r requirements.txt`
- 环境变量错误：检查 `.env` 文件

### Q2: Nginx 502 Bad Gateway

**原因**：后端服务未启动

**解决**：
```bash
sudo systemctl status mathemist-backend
sudo systemctl start mathemist-backend
```

### Q3: 前端页面空白

**原因**：静态文件路径错误

**检查**：
```bash
ls -la /opt/mathemist/frontend/out
```

**解决**：重新构建前端
```bash
cd /opt/mathemist/frontend
pnpm build
```

### Q4: API 响应慢

**优化**：
1. 检查 GPU 是否被正确使用
2. 增加工作线程数
3. 启用缓存
4. 考虑使用本地部署的大模型

---

## 🎯 上线检查清单

- [ ] 代码已上传到服务器
- [ ] Python 依赖已安装
- [ ] Node.js 依赖已安装
- [ ] 前端已构建
- [ ] `.env` 文件已配置
- [ ] Nginx 已配置并启动
- [ ] systemd 服务已配置并启动
- [ ] 防火墙已开放端口
- [ ] 可以通过公网 IP 访问
- [ ] HTTPS 已配置（如果有域名）
- [ ] 日志监控已设置
- [ ] 备份策略已制定

---

## 📞 技术支持

如果遇到问题：

1. 查看日志：`sudo journalctl -u mathemist-backend -f`
2. 检查服务状态：`sudo systemctl status mathemist-backend`
3. 测试 API：`curl http://localhost:8000/api/info`

---

## 🎉 完成！

部署完成后，你的系统将可以通过以下地址访问：

- **HTTP**: http://49.233.140.116
- **HTTPS**: https://your-domain.com（如果配置了域名和SSL）

祝你的 Mathemist 系统上线成功！🚀
