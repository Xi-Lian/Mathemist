# 文件名核心主题匹配改进总结

## 📋 问题描述

用户查询："我想要棱柱的**练习课**课件"

**之前的返回结果**：
1. ✅ `8.1课时1 棱柱、棱锥和棱台` - 得分0.735 - 包含"棱柱"
2. ❌ `8.1课时2 圆柱、圆锥、圆台和球` - 得分0.629 - **不包含"棱柱"**

**问题**：第二个课件的文件名中完全没有"棱柱"这个词，但得分仍然很高（0.629），这说明文件名评分逻辑有问题。

---

## 🔍 问题诊断

### 根本原因：关键词扩展过于宽泛

通过代码分析发现，文件名评分逻辑存在以下问题：

1. **过度依赖知识图谱的keywords扩展**
   ```python
   # "立体几何"的keywords包含：["棱柱", "棱锥", "棱台", "圆柱", "圆台", "球", ...]
   # 当核心主题是"棱柱"时，系统会查找其父主题"立体几何"的所有keywords
   # 导致"圆柱"也被视为相关词，获得0.3分
   ```

2. **缺少核心主题的强制要求**
   - 即使文件名中完全没有核心主题，也能通过其他关键词获得分数
   - 没有区分"核心主题匹配"和"相关主题匹配"的优先级

3. **查询关键词过滤不严**
   - 通用词如"课件"、"PPT"也会参与评分
   - 导致所有课件都能获得一定的基础分

### 问题分析

**案例对比**：

| 课件 | 文件名 | 包含"棱柱"? | 原文件名得分 | 问题 |
|------|--------|------------|-------------|------|
| 课件1 | `8.1课时1 棱柱、棱锥和棱台` | ✅ 是 | 0.6 | 合理 |
| 课件2 | `8.1课时2 圆柱、圆锥、圆台和球` | ❌ 否 | 0.3 | **不合理** |

课件2的文件名中只有"圆柱"（来自"立体几何"的keywords），就能获得0.3分，这导致它最终得分高达0.629。

---

## ✅ 解决方案

### 修改文件
`backend/app/core/retrieval/evaluation/courseware_evaluator.py`

### 核心改进：四层评分策略

#### 策略1：核心主题直接匹配（最高优先级）
```python
# 精确匹配（单词边界）
if re.search(r'\b棱柱\b', filename):
    score = 0.8  # 从0.6提升到0.8
    has_core_theme_match = True
```

**特点**：
- 使用单词边界正则表达式，避免部分匹配
- 得分从0.6提升到0.8，强化核心主题的重要性
- 标记`has_core_theme_match`，用于后续判断

#### 策略2：限制关键词扩展范围
```python
# 只使用与核心主题高度相关的关键词（前3个）
high_priority_keywords = keywords[:3] if len(keywords) > 3 else keywords

for keyword in high_priority_keywords:
    if keyword in filename:
        score = max(score, 0.5)  # 关键词匹配最高0.5
```

**特点**：
- 只使用前3个关键词，避免过度扩展
- 得分上限0.5，低于核心主题直接匹配的0.8
- 只在没有核心主题匹配时才启用

#### 策略3：过滤查询通用词
```python
# 过滤掉通用词，只保留有意义的词
meaningful_query_keywords = [kw for kw in query_keywords 
                             if kw not in ['课件', 'PPT', '幻灯片']]
```

**特点**：
- 排除"课件"、"PPT"等通用词
- 只保留有实际意义的关键词（如"棱柱"、"练习课"）
- 查询关键词得分权重从0.3降低到0.2

#### 策略4：无核心主题匹配的惩罚
```python
# 如果完全没有核心主题相关匹配，给予惩罚
if not has_core_theme_match and score < 0.3:
    score = score * 0.3  # 降至原来的30%
```

**特点**：
- 对于文件名中没有任何核心主题相关词的课件，大幅降低分数
- 确保不相关的课件不会获得高分

---

## 🎯 测试结果

### 测试场景

| 测试 | 文件名 | 包含核心主题? | 改进前文件名得分 | 改进后文件名得分 | 变化 |
|------|--------|--------------|-----------------|-----------------|------|
| 测试1 | `8.1课时1 棱柱、棱锥和棱台` | ✅ 是 | 0.6 | **0.800** | ⬆️ +0.2 |
| 测试2 | `8.1课时2 圆柱、圆锥、圆台和球` | ❌ 否 | 0.3 | **0.000** | ⬇️ -0.3 |
| 测试3 | `函数单调性讲解` | ❌ 否 | 0.1 | **0.000** | ⬇️ -0.1 |

### 最终得分对比

| 测试 | 改进前总分 | 改进后总分 | 变化 |
|------|-----------|-----------|------|
| 测试1（包含'棱柱'） | 0.616 | **0.648** | ⬆️ +0.032 |
| 测试2（包含'圆柱'） | 0.629 | **0.451** | ⬇️ -0.178 |
| 测试3（完全不相关） | 0.397 | **0.287** | ⬇️ -0.110 |

### 关键成果

✅ **测试1比测试2高0.197分**（0.648 vs 0.451）
- 之前：测试2得分更高（0.629 vs 0.616），排序错误 ❌
- 现在：测试1明显高于测试2，排序正确 ✅

✅ **文件名不包含核心主题的课件，文件名字段得分为0**
- 彻底解决了"圆柱"课件获得高分的问题

✅ **差距足够大，确保正确排序**
- 0.197分的差距足以让包含核心主题的课件排在前面

---

## 💡 经验总结

### 从这次测试中学到的

1. **核心主题必须强制匹配**
   - 文件名应该至少包含核心主题或其高度相关的同义词
   - 不能仅凭父主题的keywords就认为相关

2. **关键词扩展要有边界**
   - 过度扩展会导致不相关的资源获得高分
   - 应该限制扩展范围（如只取前3个关键词）

3. **通用词应该被过滤**
   - "课件"、"PPT"等词对所有课件都适用，没有区分度
   - 应该在评分时排除这些通用词

4. **惩罚机制很重要**
   - 对于完全不匹配的资源，应该有明确的惩罚
   - 避免"大家都得一点分，最后靠其他字段决定"的情况

5. **测试要验证排序正确性**
   - 不仅要看单个资源的得分
   - 更要看多个资源之间的相对排序是否正确

---

## 📊 影响范围

### 受益的场景
- ✅ 用户明确查询某个知识点时，返回的结果更精准
- ✅ 避免返回"看起来相关但实际上不包含核心主题"的资源
- ✅ 提高检索结果的相关性和用户满意度

### 不受影响的场景
- ✅ 综合性查询（如"立体几何的课件"）仍能返回多种资源
- ✅ 模糊查询（如"几何相关的课件"）仍能正常工作

### 潜在风险
- ⚠️ 可能会过滤掉一些通过其他方式相关的资源
- ✅ 但可以通过向量距离、内容匹配等其他字段补偿
- ✅ 整体相关性会提升

---

## 🔧 后续优化建议

### 短期（1-2周）
1. **监控实际效果** - 观察用户反馈和点击率
2. **调整参数** - 根据实际数据微调惩罚系数（0.3）
3. **补充测试** - 覆盖更多边界情况

### 中期（1-2月）
1. **智能同义词识别** - 使用LLM判断两个主题是否真正相关
2. **用户反馈学习** - 根据用户的点击和停留时间优化评分
3. **A/B测试** - 对比不同评分策略的效果

### 长期（3-6月）
1. **个性化评分** - 根据教师的教学风格调整严格程度
2. **持续学习** - 基于反馈自动优化关键词扩展策略
3. **知识图谱优化** - 区分"核心同义词"和"相关概念"

---

## ✨ 总结

这次改进解决了一个**系统性设计缺陷**：文件名评分过度依赖知识图谱的keywords扩展，导致不包含核心主题的资源也能获得高分。

**核心思想**：
- 核心主题必须强制匹配
- 关键词扩展要有边界
- 通用词应该被过滤
- 不匹配的资源应该被惩罚

**最终效果**：
- ✅ 包含核心主题的课件得分从0.616提升到0.648
- ✅ 不包含核心主题的课件得分从0.629降低到0.451
- ✅ 两者差距从-0.013扩大到+0.197，排序完全正确
- ✅ 用户能顺利找到真正相关的资源

这才是真正的**从测试中学习，系统性改进**！🎉
