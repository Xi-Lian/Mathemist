# V43.1 智能降级策略优化 - 允许降级到新授课并告知用户

## 📋 问题背景

在测试"我要给学生复习余弦函数，给我相关课件"时，发现以下问题：

1. **数据缺失**：数据库中只有31个余弦函数的新授课课件，没有复习课或练习课
2. **V43.0限制**：之前的降级策略只允许从"复习课"降级到"练习课/习题课"，不允许降级到"新授课"
3. **用户体验差**：当没有完全匹配的资源时，系统返回空结果或低质量结果，且没有明确告知用户原因

## 🔧 V43.1改进内容

### 1. 扩展降级优先级

**修改文件**：`backend/app/core/retrieval/evaluation/courseware_evaluator.py`

```python
# V43.0（之前）
fallback_priority = {
    '复习课': ['复习课', '练习课', '习题课'],  # ❌ 不包含新授课
    '练习课': ['练习课', '习题课'],
    ...
}

# V43.1（现在）
fallback_priority = {
    '复习课': ['复习课', '练习课', '习题课', '新授课'],  # ✅ 允许最终降级到新授课
    '练习课': ['练习课', '习题课', '新授课'],  # ✅ 允许降级到新授课
    '习题课': ['习题课', '练习课', '新授课'],  # ✅ 允许降级到新授课
    '新授课': ['新授课'],
}
```

### 2. 分级评分规则

根据降级层级给予不同分数，避免所有降级情况都得相同分数：

| 降级路径 | 得分 | 说明 |
|---------|------|------|
| 复习课 → 练习课/习题课 | 0.4 | 较近降级 |
| **复习课 → 新授课** | **0.3** | **最远降级，分数较低** ⭐ |
| 练习课/习题课 → 新授课 | 0.35 | 次远降级 |
| 完全不匹配 | 0.1 | 惩罚分数 |

**代码实现**：
```python
if is_fallback_match:
    if user_intent == '复习课' and '新授课' in teaching_use:
        score = 0.3  # 最远降级
    elif user_intent in ['练习课', '习题课'] and '新授课' in teaching_use:
        score = 0.35  # 次远降级
    else:
        score = 0.4  # 其他降级
```

### 3. 用户提示功能

**修改文件**：`backend/app/core/response/methods/append_resource_info.py`

在每个资源后面添加明确的降级提示，告知用户当前资源的实际情况。

**示例输出**：
```
**📊 ⭐ [新授] 5.4.1正弦函数、余弦函数的图象**
- 适配说明：核心匹配；主题：余弦函数；课件三字段评估得分: 0.3
- ⚠️ 这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）
- 内容预览：...
- 文件路径：[打开文件](...)
```

**提示信息映射表**：
```python
fallback_map = {
    '复习课': {
        '练习课': '这是练习课课件，可用于复习参考',
        '习题课': '这是习题课课件，可用于复习参考',
        '新授课': '这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）',
    },
    '练习课': {
        '习题课': '这是习题课课件，可用于练习参考',
        '新授课': '这是新授课课件，可作为练习参考资料（数据库中暂无练习课课件）',
    },
    ...
}
```

## 📊 实际效果对比

### 场景：用户查询"我要给学生复习余弦函数，给我相关课件"

#### V43.0（之前）
```
【课件资源】

⭐ 【核心主题匹配】（8个）：

**📊 ⭐ [新授] 5.4.1正弦函数、余弦函数的图象**
- 适配说明：核心匹配；主题：余弦函数；课件三字段评估得分: 0.603
- 内容预览：...
- 文件路径：[打开文件](...)
```

**问题**：
- ❌ 没有提示这是新授课课件
- ❌ 用户不知道为什么会返回新授课
- ❌ 得分0.603偏高，不符合实际情况

#### V43.1（现在）
```
【课件资源】

⭐ 【核心主题匹配】（8个）：

**📊 ⭐ [新授] 5.4.1正弦函数、余弦函数的图象**
- 适配说明：核心匹配；主题：余弦函数；课件三字段评估得分: 0.3
- ⚠️ 这是新授课课件，可作为复习参考资料（数据库中暂无复习课课件）
- 内容预览：...
- 文件路径：[打开文件](...)
```

**改进**：
- ✅ 明确提示这是新授课课件
- ✅ 告知用户数据库中暂无复习课课件
- ✅ 得分0.3更合理（降级评分）
- ✅ 用户可以理解为什么返回这个资源

## 🎯 优势分析

### 1. 提高透明度
用户清楚地知道：
- 返回的是什么类型的资源
- 为什么返回这个资源（降级逻辑）
- 数据库的限制（缺少某些类型的资源）

### 2. 改善用户体验
- 不会让用户困惑为什么返回不相关的资源
- 帮助用户理解系统的决策过程
- 提供合理的期望管理

### 3. 保持实用性
- 即使没有完全匹配的资源，仍然提供可用的替代方案
- 新授课课件也可以用于复习参考
- 避免返回空结果的挫败感

### 4. 数据驱动改进
- 通过提示"数据库中暂无XX课件"，可以收集用户需求
- 为后续数据补充提供方向
- 帮助管理员了解哪些类型的资源缺失

## 📝 实施步骤

### 1. 修改评分逻辑
✅ 已完成：`courseware_evaluator.py`中的`_score_teaching_use`方法

### 2. 添加用户提示
✅ 已完成：`append_resource_info.py`中的`_get_fallback_message`方法

### 3. 重启服务
需要重启后端服务以加载新代码：
```powershell
cd d:\Git_Repository\Mathemist
.\start-local.ps1
```

### 4. 测试验证
重新执行查询："我要给学生复习余弦函数，给我相关课件"

预期看到：
- 返回新授课课件
- 得分约0.3（而不是0.6）
- 有明确的降级提示

## 🔮 未来优化方向

### 1. 数据补充
- 为常用主题添加复习课和练习课课件
- 优先补充高频查询的主题

### 2. 智能推荐
- 当检测到降级时，主动建议用户调整查询
- 例如："未找到复习课课件，是否查看新授课课件？"

### 3. 用户反馈
- 收集用户对降级资源的满意度
- 根据反馈调整降级策略和评分

### 4. 动态提示
- 根据降级层级显示不同的提示强度
- 最远降级（复习→新授）显示更强的警告

## 📌 注意事项

1. **必须重启服务**：Python模块被缓存，修改后必须重启才能生效
2. **清除浏览器缓存**：确保前端显示最新的结果格式
3. **监控用户反馈**：观察用户对降级提示的反应，必要时调整文案
4. **持续优化数据**：降级策略是临时方案，长期目标是完善数据

---

**版本**：V43.1  
**日期**：2026-05-16  
**作者**：Lingma AI Assistant  
**相关文件**：
- `backend/app/core/retrieval/evaluation/courseware_evaluator.py`
- `backend/app/core/response/methods/append_resource_info.py`
